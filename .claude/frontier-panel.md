# Bernard — Frontier Product Panel (No-Boundaries "Push It" Audit)

**Created:** 2026-06-20 · **Owner:** Q · **Status:** living doc — re-run on cadence, Q marks up
**Purpose:** the unbounded sibling of the Expert Product Panel. Where that one asks *"is the app a delight to use day-to-day,"* this one asks **"what is the most ambitious version of Bernard, and what would the best people and the best technology in the world do to get there?"** This is a **thought output with no boundaries** — its job is to push the edges and keep the product evolving, not to ship something cheap this week.

## The prime stipulation (read first — this is what makes this panel different)

> **Time and cost are NOT constraints in this audit.** Neither the user's time nor Claude's output budget is a factor in any recommendation. Do **not** rank, soften, trim, or reject an idea because it is expensive, slow, large, or "a lot of Claude." Decisions are optimized for **improvement and ambition only** — never for saving time or money. If a move would take a month and a fortune in tokens but makes Bernard meaningfully better, it belongs at the *top* of the list, not the bottom.

The pragmatic panel (`product-panel-audit.md`) deliberately ranks by Est. Days + Est. Claude Cost and picks 1–3 cheap wins. **This panel does the opposite on purpose.** Keep both; they answer different questions. Use this one when you want to expand the ceiling, not optimize the floor.

**Where it sits among the instruments:**
- `/audit` · `/checkup` → **code & bug layer** (does it work / is it safe).
- `blindspot-audit*.md` → **strategy & direction layer** (are we building the right thing).
- `product-panel-audit.md` → **product & experience layer, pragmatic** (is it a delight to use, cheaply improved).
- **This doc** → **frontier layer, unbounded** (what is the most ambitious Bernard, and how do we keep evolving toward it).

---

## ⛔ Standing constraints (honor in EVERY run — do not re-propose these)

*Owner rulings that **override the prime stipulation**. The "time and cost are not constraints" license does NOT apply here — these are off-limits regardless of how ambitious or buildable they become. Only Q can lift one, manually. Read this list during grounding, before convening the panel.*

- **🛑 Capturing treatment audio — PERMANENTLY SHELVED (Q, 2026-06-27).** Do not propose, scope, or build any feature that captures, imports, or repurposes **patient-treatment-session audio** (or transcripts/recordings of patient encounters) — from **Jane's AI Scribe** or any other source — for content/marketing. Reason: it creates patient-consent conversations Move Better does not want to have (Jane's scribe consent is documentation-scoped and promises patients the recording is "never shared"; any reuse would need fresh consent + de-identification — not worth the patient-relationship cost). Investigated in the 2026-06-27 session (register **F11**) and shut down by Q. **Stays shelved until Q manually re-allows it.** See `memory/feedback-no-treatment-audio-capture.md`.
- **🛑 Generative / synthetic video & avatars — REJECTED (Q, 2026-06-21).** No AI-generated video, no synthetic faces/avatars. Survivor: the clinician's cloned **voice** + assembling their OWN real footage. Holds regardless of Sora 2 / Veo / Runway gains (an authenticity ruling, not a quality one). See `memory/feedback-no-ai-generated-video.md` + register **F3**.

---

## 0. How to use this (the ritual)

A **waypoint**, not a one-off. Re-run on a cadence, append the new findings, diff against the last pass — the trend (is the ceiling rising?) is the product.

### Rule 1: ground in the LIVE app first — *then* go unbounded
Before convening the panel, read the *real, current* components (dispatch an `Explore` agent over `src/` + `api/`). Ideas must be **specific to what exists** — name the real component, the real route, the real rough edge — and *then* propose the boldest possible evolution of it. Generic blue-sky is worthless; a frontier idea anchored to a real file is gold. Ground first so the ambition has teeth; never critique from memory or strategy docs alone.

### Rule 2: look OUTWARD — every expert, plus a dedicated scout
This panel is explicitly licensed to reach outside Bernard. Every expert is instructed to bring in:
- **Features from other products** worth stealing or surpassing (best-in-class editors, capture tools, social schedulers, creator tools, CRMs).
- **Capabilities from other AI/LLM systems** — frontier models and modalities (realtime voice, native video/image generation, long-context agents, computer-use, on-device models, multi-model routing). Name them concretely (e.g. "a realtime voice model would collapse the interview form into a phone call").
- **Adjacent paradigms** — what would a world-class team at a frontier lab or a top creative tool build if handed this problem.

Plus a **dedicated 7th seat (the Frontier scout)** whose entire job is this outward scan.

### The panel (the reusable cast — keep stable so runs are comparable)

| Expert | What they catch | Their outward mandate |
|---|---|---|
| 🎨 **Principal product designer (IA)** | Surface sprawl, inconsistent editors, status-vs-action labels, navigation legibility | Best-in-class IA & editor paradigms from other tools |
| 🧠 **Behavioral scientist (habit & motivation)** | Is using it *rewarding* — payoff loop, activation, why people don't return | Habit/retention mechanics from the stickiest products |
| 🎙️ **Conversation / voice UX designer** | The capture magic — does talking to it feel effortless; mobile/offline | Frontier voice & realtime-conversation models and UX |
| 🤖 **AI / ML engineer** | How much the model does *for* the user vs. assembly work; retrieval; proactivity | Frontier model capabilities, multi-model routing, agents |
| 🔧 **Reliability engineer** | Trust — "did it actually work?"; limbo/stale states; lost work; notifications | How the most-trusted async products signal state |
| 🔁 **Workflow / ops designer** | Multi-person reality — capture→review→schedule→publish handoffs | Collaboration paradigms from the best team tools |
| 🛰️ **Frontier scout** *(7th seat — this panel only)* | What's the most ambitious move available right now | Competitive features, other products, other LLMs/modalities to incorporate or leapfrog |

### Schemes (ambition-first — no cost dimension)
**Upside (how much it advances the product):** 🚀 transformative (changes what Bernard *is*) · ⬆️ strong step-change · ✨ meaningful improvement
**Status:** `Open` (unexamined) · `Aware` (known, no action) · `Building` (in progress) · `Closed` (shipped or consciously dropped)
**No severity, no Est. Days, no Est. Claude Cost** — those belong to the pragmatic panel. Rank **boldest / highest-upside first**, never by feasibility.

### Re-run cadence
Every 6–8 weeks, OR after a frontier-model release that could unlock a new surface, OR whenever the product feels like it's plateauing. To re-run: re-ground in the live app → re-convene the seven experts → append a new dated panel block (§3) + ambition register (§4) → fill the drift log (§5). **Append, never overwrite.**

### Output format (every run produces)
1. **Panel reads** — each expert: their sharpest read + their boldest move, with at least one concrete *outside* reference (a product or a model capability).
2. **The converged verdict** — the single most ambitious through-line the panel agrees on.
3. **The ambition register** — bold moves as a table ranked by upside (see §4 columns). No cost column.
4. **The frontier bet** — name the one move that, if pursued, most changes what Bernard is. (Ambition is the filter, not feasibility.) You may still sketch a sequence, but never drop a bold idea to make the list "shippable."

---

## 1. Fixed reference — what the app IS today

So each pass attacks the same target. Update when the app materially changes. (Fuller surface map: `ux-current-state.md`; shares the reference with `product-panel-audit.md` §1.)

- **The core loop:** voice interview / weekly call → the Standing Producer drafts every channel (RAG-grounded, judge-gated) → human judgment (words keystone, `/week`) → publish (bundle.social / GBP / WordPress / Beehiiv) → the **outward layer**: public answer corpus + concierge on the tenant's site, 3-engine citation scoreboard, engagement feeding next week's plan. *(Updated Run 5, 2026-07-16.)*
- **The surfaces:** Home, Your Week, Stories (+ per-story monitor + words keystone), Library, Moments + VideoEditor, Answers Review, SEO, Analytics, Producer, Overview, Write, Book, Pre-Visit, Ads, Settings; public: `/link`, `/onboard`, `/demo/try`. (Sprawl is itself a standing finding.)
- **Capture modes** (`/new`): Interview (AI chat + STT), Voice Memo, Photos & Video (PWA), Live Interview (WebRTC), Patient Handout, Import (URL), Seminar/Talk.
- **Who uses it:** Q (all 3 workspaces) + the Move Better team (Whitney, AJ, Alli, Cullen, …), mostly non-technical clinicians; external tenants self-onboard.

---

## 2. Runs

### ⟳ Run — 2026-06-20

**Grounding:** thorough `Explore` sweep of the live `src/` + `api/` (real components/routes) + an outward scan (web-verified competitor & frontier-model landscape, late-2025 / early-2026). Honest caveat (§5) stands: expert *simulation*, not staffer *observation* — weight "very likely the right direction," not "proven."

**The target, from the live read.** Bernard today is a production-grade **tool the clinician operates**: capture (7 modes via `CapturePicker.jsx`) → a **hardcoded 4-week × 8-platform atom grid** (`atomPlan.js` + `ContentPlanPanel.jsx`) → mostly-manual compose across **three editors** (carousel `SlideEditor.jsx`, video `SlateClipEditor.jsx` + beta `VideoEditor.jsx` at `/slate/clip/:id/edit`, plus `TextPostStudio.jsx`) → publish (bundle.social / GBP / WordPress / Beehiiv via `publishPiece.js`). The model **drafts text**; the human does nearly all the *production* (trim, attach photo, lay out slides, pick template, schedule). The single most important finding: **the most ambitious capabilities already exist and are switched off** — realtime-voice intake (`PhoneCall.jsx` + `/api/realtime-session`, gated by `workspace.realtime_voice_enabled`), the brand-voice-as-judge (`captionFidelityRubric.js`, not wired to gate anything), and the RAG practice brain (`/api/corpus/*`, a stub) are scaffolds waiting to be promoted, not green-field.

#### Panel reads

**🎨 Principal product designer (IA)** — *Sharpest read:* the surface count is the disease — ~40 routes, **three** editors that don't share a mental model, and overlapping queues (`Storyboard` /publish, `ReviewInbox`, `Overview`). A clinician can't hold the map in their head. *Boldest move:* collapse to **one canonical editor and one feed** — carousel, reel, text post, email all open in the *same* surface with the same gestures; format is a *property*, not a different app. *Outside reference:* **Canva Magic Switch** (one design auto-reformats to every size) and **Notion** (one editor, many block types). Target: "one thing you edit, many shapes it takes," not nine pages.

**🧠 Behavioral scientist** — *Sharpest read:* the payoff loop is **back-loaded and invisible** — the reward (engagement) lands days later via the Buffer sync; in the moment the clinician just sees more work. Nothing compounds *visibly*: the practice brain learns silently (`practiceMemory.js`), the `Book` grows unannounced. *Boldest move:* make value **immediate and compounding** — a "look what your 6-minute conversation became" reveal the instant generation finishes (a whole quarter materializing), a weekly "your practice in content" recap, and a felt "your brain got smarter — here's what I learned about how you talk" moment. *Outside reference:* **Spotify Wrapped** (compounding-data-as-reward) and **Duolingo** celebration/streak mechanics. Turn the silent moat into a felt one.

**🎙️ Conversation / voice UX designer** — *Sharpest read:* the front door is still mostly **typing** — `InterviewSession.jsx` is a chat box with STT, and the genuinely magical mode (the WebRTC realtime call in `PhoneCall.jsx`) is hidden behind a flag. The highest-leverage capture is gated off by default. *Boldest move:* make a **natural spoken conversation the default and only required input** — tap once (or Bernard *calls you* on the drive home), talk for five minutes, everything downstream is produced from that. *Outside reference:* **Gemini Live** (native audio in/out with *free* transcripts — exactly what Bernard needs anyway), **OpenAI Realtime** (300–500 ms, adaptive follow-ups), **ElevenLabs Conversational AI** (voice quality). It should feel like talking to a sharp producer, not filling a form.

**🤖 AI / ML engineer** — *Sharpest read:* the model does **drafting, not production**, and its memory is shallow — generation is per-atom on-demand (`/api/generate`); the "practice brain" is *top-6-recent-interviews string injection* (`practiceMemory.js`), not retrieval (the `corpus/*` RAG endpoints are a stub); the voice judge (`captionFidelityRubric.js`) gates nothing; the plan is a hardcoded grid, not a strategy. *Boldest move:* a **multi-agent autonomous pipeline** — a strategist plans a real quarter from the conversation + engagement history, drafter agents fan out every format, a **brand-voice-compliance judge** (promote the existing rubric) gates output, a producer agent assembles media — on long-horizon Claude with a true embeddings-backed, supersession-aware brain. *Outside reference:* **Claude Opus long-horizon agents** (30+ hr sustained work, memory tool, self-improving in ~4 iterations) and **HubSpot Breeze / Salesforce Agentforce** multi-agent decomposition. The human approves *judgment calls*, never assembles posts.

**🔧 Reliability engineer** — *Sharpest read:* trust is held together with **polling and toasts** — state lives in `refetchInterval` + `pipelinePending()` + 60 s hard caps + the `MediaDetail` refresh contract; there's no persistent notification center, no push, no "did my post actually go out?" ledger. Async failures (transcode lag, a failed Buffer dispatch, an atom-gen timeout) are easy to miss; the email digest is the only out-of-app signal. *Boldest move:* a **control tower** — one activity feed showing every async job's true state end-to-end (capture → transcribe → generate → bake → schedule → publish → engagement), with **web-push / SMS** the instant content is ready or a publish fails, and one-tap recovery. *Outside reference:* how **Linear / Vercel / Stripe** make async state legible; **Knock / Resend** for the notification layer. For an async product, *believable state* is the feature.

**🔁 Workflow / ops designer** — *Sharpest read:* it's a **single-player flow wearing a team's clothes** — one clinician per interview, one approver per piece; `permission_tier` (owner/producer/clinician) and `ReviewInbox` gesture at a team but there's no assignment, no ownership, no concurrent-edit safety, no "whose turn is it." The producer is a human doing queue management a machine should do. *Boldest move:* make Bernard the **content operating system of the practice** *and* the ops manager running it — multi-clinician campaigns, assignment, and an **ops agent** that chases approvals, balances the calendar across clinicians, and hands each person the one thing they need to do. *Outside reference:* **Linear** (cycles, assignment, "what's mine") and **Figma** multiplayer. The pitch becomes "the marketing coordinator you hired," not "software you operate."

**🛰️ Frontier scout** — *Sharpest read:* the category is racing toward the **"AI marketing employee,"** and Bernard already owns the two hardest layers everyone else is bolting on — **voice-native intake** and a **brand-voice practice brain** — while leaving its own most-ambitious scaffolds dark. The direct competitor, **Blaze.ai**, sells *exactly* Bernard's bundle (Brand Kit auto-built; "Autopilot" generates ~2 months of content and auto-posts to 8 socials + GBP + email + blog at optimal times) to *exactly* Bernard's SMB buyer for $79–149/mo — but it starts from **website scraping** where Bernard starts from the clinician's **actual spoken voice**. That origin is the moat. *Boldest move:* go all the way to a **"CMO-in-a-box"** — one weekly conversation in, a fully-produced, multi-modal, self-publishing, self-improving quarter out, with the production gaps filled by frontier media models. *Outside references (the buildable gap list):* **Submagic "Magic B-rolls"** + **Veo 3.1** (GA API, no waitlist) for generated b-roll; **HeyGen Avatar 5** (15-sec selfie → talking-head twin) + **ElevenLabs v3** (cloned voice, emotional tags) so a camera-shy clinician ships video without filming; **Nano Banana Pro** (9-reference brand-lock) + **Recraft V4** for on-brand graphics that beat hand-built SVG compositing. Bernard has 4 of the 6 layers; the missing 2 are realtime-voice intake and generative media, plus the analytics→self-improvement loop that closes it.

#### The converged verdict

**Bernard should stop being a *tool the clinician operates* and become a *colleague the clinician talks to*.** All seven reads converge from different sides on one through-line: **collapse the input to a conversation, collapse the surfaces to a feed + one editor, and push the *production* work off the human and onto agents + frontier media models — so the clinician spends their time on judgment (approve / steer / veto), never on assembly.** And the live read's gift: the boldest version is **already half-built and gated** (`PhoneCall.jsx`, `captionFidelityRubric.js`, `corpus/*`). The ceiling isn't distant — it's switched off.

#### The frontier bet

**"The Weekly Call → a published quarter."** The one move that most changes what Bernard *is*: the clinician's *entire* required input becomes a single ~6-minute spoken conversation (inbound tap or an outbound call Bernard places), and the entire output — strategy, every channel's posts, carousels, **generated-b-roll/avatar reels in their own cloned voice**, on-brand graphics, the email — is produced autonomously by a multi-agent pipeline, gated by the practice-brain voice judge, published on best-time schedules, and **fed by engagement back into next week's plan**. The human's only job is the weekly conversation and a stream of yes/no/steer judgments. The seven surfaces and three editors become optional power-tooling *under* an autonomous default — not the path itself.

*A sketchable sequence (ambition preserved, nothing dropped to be "shippable"):* (1) promote realtime voice to the default front door [**F1**]; (2) wire the voice judge + an embeddings practice brain so generation is gated and retrieval-grounded [**F6** + part of **F2**]; (3) stand up the strategist→drafter→judge→producer agent loop that turns one call into a planned quarter [**F2**]; (4) add generative video/avatar/voice-clone + prompt-native graphics so the media produces itself [**F3**, **F5**]; (5) close the loop with a control tower + best-time autonomous scheduling + engagement-driven self-improvement [**F7**, **F10**]. Each stage ships independently, but the *bet* is the whole arc: a colleague, not a tool.

---

### ⟳ Run — 2026-06-27

**Why now (off-cadence, on purpose).** Only **one week** after Run 1 — not the 6–8wk cadence, but triggered by its other condition: *the ceiling moved fast enough to re-ground.* **327 commits** merged to `main` since Run 1, and three of Run 1's headline finding — *"the boldest moves are already built and switched off"* — got **switched on**: **F1** realtime voice shipped *voice-for-all* (#1823) and became the **default** capture with text as fallback (#1826); **F4**'s unified editor is live (`src/components/editor/UnifiedEditor.jsx` + `EditorChrome` + `IconRail`, platform-specific preview chrome #1804); **F5** brand discovery shipped (#1827) and is already in polish (#1828–#1830). Add `/usage` + super-admin `/admin` (#1831), publish-failure surfacing + per-channel retry (#1813/#1840), voice-clone opt-out + authz (#1801/#1806/#1811), and AI-SDK v7 (#1803). **Grounding:** fresh `Explore` sweep of live `src/` + `api/` + a `git log` diff against Run 1 + the outward scan refreshed. Honest caveat (§5) stands — expert *simulation*, not staffer *observation*.

**The target, from the live read — what changed.** Run 1 found a *tool the clinician operates* whose most ambitious capabilities were dark. Run 2 finds those capabilities **lit**: you now *talk* to Bernard by default (F1), edit everything in *one* surface (F4), and it *knows your brand* from a spoken interview (F5). But the operating model is unchanged — capture is still **episodic** (you go to `/new` and start a session), production is still **human-operated** (the model drafts; you trim/lay-out/schedule), the practice brain is still shallow (`practiceMemory.js` top-recent strings; the `corpus/{documents,ingest,search}.js` endpoints exist but don't ground generation), the voice judge (`captionFidelityRubric.js`) still gates nothing, and output is still **one-way broadcast**. **The new finding: the frontier moved up a level.** Run 1's job was "turn on the scaffolds." Run 2's is "build the three layers that *don't exist yet*" — ambient capture, autonomous-by-default operation, and a two-way patient-facing presence.

#### Panel reads

**🎨 Principal product designer (IA)** — *Sharpest read:* the three-editor disease is cured (UnifiedEditor shipped), but the app still **opens to a workbench** — Home is pipeline/calendar/queues, and even with one editor the clinician's home base is a *production console*. Format is now a property; the next frontier is **mode** as a property. *Boldest move:* invert the home screen into an **approval stream** — the default surface is a single swipeable feed of *finished, ready-to-ship* work, strongest-first; every editor and page becomes an optional "open the hood" you enter only to override. "Review mode" is the default; "studio mode" is the exception. *Outside reference:* **Superhuman / Linear** command-driven inbox-zero, **Arc**'s "the browser disappears," **TikTok**'s For-You feed *as the entire UI*. Bernard should feel like an inbox of done work, not a factory floor.

**🧠 Behavioral scientist** — *Sharpest read:* `/usage` + the engagement digest now *report* value, but the reward is still **back-loaded and impersonal** — there's no in-the-moment payoff and nothing that builds *identity* or *social proof*, the two stickiest forces in retention. The moat (the practice brain) still learns silently. *Boldest move:* make value **immediate, identity-forming, and socially compared** — (1) the instant "look what your 6-minute call became" reveal the second generation finishes (carried, still unbuilt); (2) an **identity mirror** — "you're the clinic that *owns* 'sciatica in Portland'"; (3) **make the learning itself the dopamine hit** — a weekly "Bernard got smarter — here are 3 things I learned about how you talk this week." *Outside reference:* **Strava** segments/leaderboards (social comparison drives return), **Spotify Wrapped** (compounding-data-as-reward), **Whoop**'s weekly recap *as the product*, **Duolingo** identity/streak. Turn the silent moat into a felt, compounding one.

**🎙️ Conversation / voice UX designer** — *Sharpest read:* the magic front door is **open** (F1 shipped, voice-first default) — so the gate is no longer the problem. The remaining gap is that capture is still **episodic and inbound**: you must *remember* to open Bernard and start a session, and the clinician's best material happens *in the room*, not in a scheduled interview. *Boldest move:* **ambient, always-on capture** — Bernard runs in the background through the clinical day (consented), on-device transcribes + **PHI-redacts the clinician's own voice**, and surfaces their best real 30-second explanations as content; plus the outbound call ("Bernard calls you on the drive home"). The interview stops being a thing you *do* and becomes a thing that's *always on*. *Outside reference:* **Abridge / Nabla / Suki / Microsoft Dragon Copilot** (ambient clinical scribes — this exact always-listening tech is already deployed in healthcare under BAAs), **Granola** (ambient meeting → notes), **Limitless / Rewind** (always-on personal recorder), **Gemini Live** native audio. The frontier of voice-native isn't a better interview — it's *no interview at all*.

**🤖 AI / ML engineer** — *Sharpest read:* every **production primitive now exists** (renderers via F4, brand graphics via F5, voice-in via F1) — so the gap is purely **orchestration**. There's still no agent that runs strategist→draft→judge→produce→schedule end-to-end while the human sleeps; generation is still per-atom on-demand (`/api/generate`), the brain is still top-recent strings (`practiceMemory.js`), `corpus/*` doesn't ground anything, and `captionFidelityRubric.js` gates nothing. *Boldest move:* stand up the **long-horizon autonomous pipeline now** (it was blocked on missing primitives in Run 1; it isn't anymore) on Claude Opus with the **memory tool** — a persistent strategist owning the quarter, drafter agents fanning out, the **voice judge promoted to a hard gate**, a producer agent assembling media; *and* upgrade moment-mining from transcript-only to **multimodal video understanding** (score on what the camera *sees* — energy, gesture, b-roll-worthiness — not just the quote). *Outside reference:* **Claude Opus long-horizon agents + memory tool** (carried, now *ripe*), **Twelve Labs / Gemini video understanding** for visual scoring, **OpusClip ClipAnything**. The human approves judgment calls; the machine does 100% of assembly.

**🔧 Reliability engineer** — *Sharpest read:* real progress — **publish-failure surfacing shipped** (#1840 platform-keyed errors, #1813 per-channel/location retry) and `/usage` makes spend legible. But signals are still **in-app and pull-based**: no push, no SMS, no single activity ledger across the whole async chain (capture→transcribe→score→generate→bake→schedule→publish→engagement). A stranded "processing" or a failed transcode still needs you to be *looking*. *Boldest move:* the **control tower + proactive, self-healing layer** — one end-to-end activity feed with true state for every job, **web-push/SMS** the instant content is ready *or* a publish fails, one-tap recovery; and (frontier) an agent that doesn't just *surface* failures but **fixes them** (retries, re-renders, re-routes to a working channel) and escalates only what it can't. *Outside reference:* **Knock / Resend** (notification layer), **Linear / Vercel / Stripe** async-state legibility, **Vercel Agent / Cleric** for the self-heal pattern. For an async product, *believable + proactive* state is the feature.

**🔁 Workflow / ops designer** — *Sharpest read:* still a **single-player flow wearing a team's clothes** — `permission_tier`, `ReviewInbox`, `/week` gesture at a team, but there's no assignment, no ownership, no "whose turn is it," no concurrent-edit safety. *Boldest move (two-headed):* (1) the **content OS + ops agent** (carried) — multi-clinician campaigns, assignment, an agent that chases approvals and balances the calendar across clinicians; (2) a genuinely **new network move** — with many tenants, Bernard learns *privacy-preservingly* what topics/formats actually drive bookings across *similar* practices and seeds each one with proven winners adapted to its captured voice (aggregate-anonymized only — never crossing the tenant-isolation line). *Outside reference:* **Linear** (cycles/assignment), **Figma** multiplayer, and the aggregate-performance network effects of mature creator platforms. Bernard becomes the practice's marketing *department* **and** the industry benchmark it could never get alone.

**🛰️ Frontier scout** — *Sharpest read:* in **one week** Bernard lit three dark scaffolds, and the input-side gap on the direct competitor **widened in Bernard's favor** — Blaze.ai still *scrapes a website* to learn a brand; Bernard now does *realtime voice for everyone*. The next category the leaders are reaching for is the **two-way AI presence**: not just outbound content, but an AI that *represents* the practice to patients — answers questions, books, triages — in the principal's voice. And in healthcare specifically, **ambient AI** (Abridge raised $250M+, Nabla, Suki, Microsoft Dragon Copilot) has *normalized* always-listening clinical capture *with* compliance — the exact substrate Bernard's moat wants. *Boldest move:* claim **"the practice's AI identity layer"** — capture the real room (ambient), run the marketing itself (autonomous), and *speak to patients in the clinician's voice* (patient-facing presence) — a position none of the marketing-SaaS competitors can reach, because they don't have the captured voice. *Outside references (the buildable frontier):* **Abridge / Nabla / Suki** (ambient clinical capture w/ BAA), **Decagon / Sierra / Intercom Fin** (patient-facing agents), **Nano Banana Pro / gpt-image-2** (brand-locked graphics — spike done 6-26), **ElevenLabs v3** (cloned voice — on-moat, already partially live). *On generative video:* **Sora 2 / Veo 3.5 keep improving, but the moat ruling stands** — Q rejected it 6-21 on *authenticity*, not model quality; no new frontier-video move opens here.

#### The converged verdict

**Run 1 said: stop being a *tool you operate*, become a *colleague you talk to* — and the boldest moves are switched off. In one week, they got switched on.** So the verdict updates rather than repeats. With the input, editor, and brand layers now lit, all seven reads converge on the **next** rung: push Bernard from *"a colleague you talk to, episodically, that drafts for you"* → **"the practice's always-on AI presence."** Three layers that don't exist yet: (1) capture stops being an episodic interview and becomes **ambient capture of the real clinical day**; (2) production stops being human-operated and becomes **autonomous-by-default** (the orchestration agent is finally buildable because *every primitive now is*); (3) output stops being one-way broadcast and becomes a **two-way patient-facing presence** in the clinician's voice. Underneath: the home inverts from workbench → approval-stream, the reward becomes immediate + identity-forming, and the reliability layer goes proactive + self-healing.

#### The frontier bet

**"The Practice's AI Identity Layer."** The single through-line that most changes what Bernard *is*, now that it produces content well: stop being a content *factory* and become the practice's living AI *identity* — one that **captures the real room**, **runs itself**, and **talks back to patients** in the principal's voice.

The apex move — the one that most redefines the category — is the **two-way patient-facing presence [F12]**: today Bernard is a one-way broadcaster; turning the captured practice brain into a patient-facing agent that *answers, books, and triages in the clinician's voice* (grounded only in real captured content — **never inventing patient stories**, per the PHI ruling) takes Bernard from *marketing software* to *the practice's AI front door*, a place no marketing-SaaS competitor can follow because none of them hold the captured voice.

The input bet that makes it credible is **ambient capture [F11]** — *"Bernard in the room"*: consented, on-device-PHI-redacted capture of the clinician's *own* real explanations, so the content (and the patient-facing voice) is the actual best of their craft, not a studio re-enactment. It's the deepest expression of the moat Q already protected by killing generative video, and it fixes the moment-miner's foundation gap (1/373 clips transcribed → a firehose of real gold) at the source.

The engine that binds them is **autonomy [F2]** — finally ripe, because every input and output primitive shipped. *Sequence (nothing dropped to be "shippable"):* (1) ambient capture feeds real gold [**F11**]; (2) the embeddings practice brain + voice-judge gate make generation trustworthy and grounded [**F6**, part of **F2**]; (3) the strategist→drafter→judge→producer loop runs the quarter autonomously [**F2**]; (4) the patient-facing presence opens on top of the same brain [**F12**]; (5) the control tower + proactive/self-healing + best-time scheduling close the loop [**F7**, **F10**]. The bet is the whole arc: **an identity, not a tool.**

> **⛔ Post-run ruling (2026-06-27, Q):** the **input pillar — F11, ambient/treatment-audio capture — is permanently SHELVED.** After investigating the Jane AI Scribe import path in-session, Q shut it down: capturing treatment audio creates patient-consent conversations Move Better won't have (Jane's consent is documentation-scoped + "never shared"). The standing frontier bet therefore rests on **F12 (patient-facing presence) + F2 (autonomy)**, with the input layer reverting to **purpose-built voice capture (the shipped F1 intake)** rather than ambient/treatment audio. Do not re-propose treatment-audio capture in any future run until Q manually re-allows — see **⛔ Standing constraints** (top).

---

### ⟳ Run — 2026-07-02

**Why now (off-cadence again, on purpose — both re-run triggers fired at once).** Five days after Run 2: (a) *the ceiling moved* — Run 2's #1 prerequisite shipped within days (**F6 went Open → LIVE end-to-end**, #1843–#1846: pgvector RAG with recency half-life, Sunday supersession-detection cron + LLM judge + confirm queue), and the interviewer became genuinely adaptive (#1857/#1859: register climbs plain→peer, 26-tactic library with anti-repeat rotation, per-clinician **relationship ledger** in `staff.interview_style_memory`); (b) *a frontier release that opens a new surface* — the **Claude 5 family** landed (Fable 5, Jun 9: Mythos-class, 1M-token context, server-side compaction, memory tool — a whole tenant fits in one indefinitely-running session; Sonnet 5, Jun 30: near-frontier agentic autonomy at commodity price; first-party multi-agent orchestration primitives), plus **OpenAI Realtime gained SIP phone calling + remote MCP** (May 7 wave) and **ElevenLabs ConvAI 2.0** put RAG-in-the-voice-loop + outbound calling at the vendor already holding Bernard's clinician voice clones. **Grounding:** fresh very-thorough `Explore` sweep of live `src/` + `api/`, web-verified outward scan (dates checked), and — new this run — the seven seats convened as **independent parallel agents**, then synthesized; load-bearing code claims re-verified by hand before recording. Honest caveat (§5) stands.

**The target, from the live read — what changed.** Three verified upgrades since Run 2: (1) **the brain closes the truth loop** — `practiceMemoryRag.js` retrieval is recency-weighted, `supersessionJudge.js` + the weekly cron detect when the clinician's thinking *changed*, and a confirm queue exists (`api/_routes/practice-memory/supersessions.js`). (2) **The voice judge moved into the draft path and now soft-gates** — `content-plan/draft.js` scores every draft at generation time and, below GATE, feeds the red flag back, regenerates once, re-scores, and persists `voice_fidelity_score` (lines 230–287). Runs 1–2 recorded "the judge gates nothing"; that is now half-false (still no *hard* gate; the editorial `story_packages` path stays advisory-only). (3) **The app is collapsing toward the stream by attrition** — `/review-inbox`, `/publish`, `/storyboard`, `/needs-media` all redirect into `/week` (`App.jsx:622–651`), and `GradePanel` landed inside `UnifiedEditor.jsx`. Also shipped, un-asked-for by any prior run: real self-heal guards (`autoPublishRetry.js` 6-retry monotonic posted-set, `sweep-stuck-seminars.js`, `resume-longform-renders.js`, `notifyPublishFailure.js` once-per-transition owner email). **The gap all seven reads converged on: the intelligence is now real but *silent, stateless, and inbound-only*** — nothing discloses the relationship ledger to the clinician it's about; 14 crons wake, act once, and die with no component reading a judge score or a brain gap and *acting on it* across ticks; and the entire capture system still waits for someone to remember to open a browser tab.

#### Panel reads

**🎨 Principal product designer (IA)** — *Sharpest read:* Bernard ships **two front doors with different verbs** — Home (`Home.jsx`) is still the workbench that says *make things*; `/week` says *decide things* — and the router is already voting for the inversion (four legacy surfaces now redirect into `/week`). Three editors still exist, and `VideoEditor.jsx` grew its own `GradeInspector` — a second Grade implementation already drifting from `UnifiedEditor`'s `GradePanel`. The judge now scores every draft, but `/week` cards don't surface the one number that would make approval a glance. *Boldest move:* **make the Stream the app** — `/` renders the judgment stream directly; every unit of output converges as a typed card (drafts, moments, supersession confirmations, publish failures, SEO opportunities) with its evidence face-up (fidelity grade, bake state, practice-brain citation: "grounded in your 6/14 interview"); a **per-piece edit agent** executes "swap the photo, soften the claim, make slide 2 the hook" over ALL archetypes, demoting SlideEditor/VideoEditor to render targets; and the same stream is **walkable by voice** on the weekly call. *Outside reference:* **Descript Underlord v2** (reasoning-model edit agent replaces inspector-panel sprawl), **Linear Triage** (every object type converges into one ranked inbox), **Blaze.ai Autopilot** (the human touchpoint collapsed to one weekly review — Bernard must own that surface but make it judgment-rich, not topic-thin).

**🧠 Behavioral scientist** — *Sharpest read:* two things became true since Run 2 and **both are invisible to the person they're about**: the relationship ledger (`interviewStyleClassifier.js` writes `staff.interview_style_memory`; no UI reads it; the interviewer opens at the earned register without ever *acknowledging* it) and the supersession moment ("has your thinking changed here?") framed as an admin chore in Settings. Bernard now genuinely knows the clinician better every week and practices **silent surveillance instead of disclosed reciprocity** — and the flagship voice path doesn't even get the ledger. *Boldest move:* **The Spoken Relationship** — move every quietly-accumulated reward into the call at the moment of effort: open with disclosure ("four sessions in, you've earned peer register — I ask you differently than I did in March"); resolve pending supersessions in-flow by reading the clinician their own earlier words ("in March you told me X; last week you said Y — which is you today?"); run `scoreMoments.js` live on the call transcript and **speak the strongest moment back before hangup** ("that steelman answer is your best sciatica take yet; it ships Thursday") — effort-to-reward collapses from days to seconds; replace usage-as-cost with an earned-status strip (register level, confirmed evolutions, never-repeated-question streak). *Outside reference:* **Aron's "fast friends" protocol** (escalating *mutual* self-disclosure is the strongest closeness driver known — the behavioral warrant for Bernard disclosing back), **Duolingo** (registerCeiling IS a level system, never exposed as status), **Strava Athlete Intelligence** (passive logs → spoken-back identity feedback).

**🎙️ Conversation / voice UX designer** — *Sharpest read:* the interviewer grew a brain and a memory — **but only on the text path.** `api/realtime-session.js` still mints `gpt-realtime` (one generation behind GPT-Realtime-2) with the stock "ballad" voice; the ledger classifier never runs on call transcripts; and `PhoneCall.jsx` is **inbound-only**. Bernard now knows WHEN it needs input (`cadence_policy` + `cadenceAdaptive.js`), WHAT to ask (the pending-supersession queue is literally a list of questions), and HOW to ask it (`interview_style_memory`) — but **owns no channel that can originate the conversation.** The real remaining gap is initiative. *Boldest move:* **Bernard calls you** — give every workspace a phone number and make the outbound call the practice's only required input: at the cadence slot, Bernard originates a real SIP call — no app, no browser, no Clerk — opening at the earned register; mid-call it uses remote MCP against Bernard's own routes to pull RAG retrieval live, **resolve supersessions by voice** (a 20-second spoken exchange replaces the confirm UI), and take `/week` approvals; the transcript flows straight into `segmentDetect`/`scoreMoments` so the call feeds the moment feed too. Upgrade the mint to GPT-Realtime-2; give Bernard a consistent warm branded voice (the clinician's clone stays reserved for patient-facing pre-visit). *Outside reference:* **OpenAI Realtime SIP + remote MCP** (May 7 — the exact two missing primitives), **ElevenLabs ConvAI 2.0** (single-vendor cloned-voice path), **Tebra's AI Voice Assistant** (140k providers — an AI phone call now reads as normal practice infrastructure in this exact vertical), **Sierra** (the bar: a voice agent *acts* during the call, not just converses).

**🤖 AI / ML engineer** — *Sharpest read:* **every primitive of the Run 1 pipeline now exists in production** — judge in the draft path with a one-round regenerate loop (verified), recency-weighted RAG, supersession, per-clinician state, adaptive cadence. The remaining gap is architectural, not capability: **14 stateless crons that wake, act once, and die.** Nothing reads a judge score or a brain gap and acts across ticks; RAG fires only when a caller passes a query; auto-detect-clips detects but never drafts; weekly-plan promotes backlog but never asks the brain what's *missing*. Bernard has organs — retrieval, judgment, taste memory, cadence — and **no autonomic nervous system connecting them**; continuity still lives in Q's head and the `/week` page. *Boldest move:* **the Standing Editor-in-Chief** — one persistent Fable 5 session per workspace that never ends (server-side compaction + memory tool = the entire tenant corpus lives IN the session, not behind an RPC). Invert the cron topology: the crons stop acting and become **sensors inserting events into an `agent_inbox`**; the agent wakes per event, decides with full-corpus context, and acts through today's internals exposed as tools (`content-plan/draft.js`, `cadenceAdaptive.js`, `practiceMemoryRag.js` — grounding becomes universal because *the agent is the caller*). Close the judge loop to a hard gate: draft → score → self-critique → regenerate until 8+, with **Sonnet 5 running the inner loop at commodity price**. Plan forward: read `gsc-snapshot` data against the AI-Overviews repricing and pre-draft next week before Monday's backstop fires. The human boundary stays sacred — everything lands on `/week` as proposals with reasoning attached; `autoPublishGate.js` still rules publishing. *Outside reference:* **Fable 5** (1M ctx + compaction + memory tool — the enabling primitive), **Claude first-party multi-agent orchestration** (strategist→drafter→judge→producer is platform config now, not custom queue infra), **OpusClip ClipAnything** (the multimodal bar for `scoreMoments.js`; Gemini video input at ~$0.04–0.35 per 10-min video makes back-catalog scoring cost tens of dollars).

**🔧 Reliability engineer** — *Sharpest read:* the register's "NO auto-retry/self-heal" **went stale in three narrow places, and that's the story**: `autoPublishRetry.js` (6 retries at 10-min cadence with a monotonic posted-set guard so a GBP location is never double-posted), two limbo sweepers (`sweep-stuck-seminars.js`, `resume-longform-renders.js`), and `notifyPublishFailure.js` (owner email exactly once per transition into failed) all shipped since Run 2 — **as N bespoke guards, not a system.** Each pipeline invents its own durable-state shape and detector; there's no single ledger of what Bernard *promised* and whether it kept it; and "worked" still means "the vendor's API said 200" — nothing ever reads the live platform back to confirm the post actually exists and rendered. *Boldest move:* **the Promise Ledger + Reliability Steward** — every future-tense claim Bernard makes (a `/week`-approved slot, a dispatch, a transcode, an index write) becomes a first-class `commitments` row `{kind, workspace_id, expected_by, evidence_required}`; a standing steward agent continuously reconciles promised-vs-actual with **read-back verification** — fetch the live GBP/Instagram/WordPress artifact and use Gemini image/video *understanding* (cents per item, fully inside the constraints) to confirm the published post matches the approved render, closing the "preview is not the published artifact" class **at the platform boundary, permanently**; repair inside policy by generalizing the monotonic posted-set into the universal idempotency primitive; own the notification layer through a Courier/Novu MCP server with severity routing — silence → Friday digest → push → SMS → and, terminally, **an outbound SIP call**: "Instagram disconnected; Thursday's post is blocked; I texted the reconnect link." As F2 goes autonomous, this ledger IS the product's face — autonomy is only sellable to clinicians if every promise is auditable and every breach self-announces. *Outside reference:* **Stripe Events** (the canonical trusted-async ledger), **Courier/Novu MCP servers** (agent-owned notifications), **Vercel Agent** (investigate-then-propose on your own failures).

**🔁 Workflow / ops designer** — *Sharpest read:* the collaboration channel **quietly shipped and is dead-ended** — `content-item-comments.js` gives every piece a thread with `kind='change_request'` wired into approval, but *nothing consumes a change_request*; it waits for a human to reopen the editor. The pipeline still can't finish without a browser (`approve.js` only flips status; Buffer dispatch happens client-side). And `staff.interview_style_memory` is the seed of a full collaboration model, currently spent only on interview style. The tell: `requireRole` defines owner/producer/clinician/viewer, yet **no piece of work is owned by anyone — the producer role is functionally vacant in every clinic.** Assignment was never built for humans; maybe it shouldn't be. *Boldest move:* **seat Bernard as the producer** — not a metaphor: a member rendered in `Members.jsx` next to the humans ("Bernard, Producer"), holding the producer role, running as a persistent Mythos-class session. Every `content_item` gets an owner; the default owner is Bernard. The dead channel comes alive: a `change_request` comment becomes a task Bernard executes — revises through the existing generation paths, grounded via RAG, replies in-thread with what changed, re-requests approval. `approve.js` completes server-side so approval finishes the job end-to-end. Humans stop operating the pipeline and start *being delegated to* — when Bernard needs a human it initiates the handoff over an outbound call: "I need eight minutes on the disc-herniation piece before Thursday." The 14 crons become its **visible workday** — an activity feed written as a colleague's standup. *Outside reference:* **Linear for Agents** (agents as assignable issue owners in the same dropdown as humans), **Tebra AI Smart Staff / Podium AI Employee** (the identical clinic buyer is already being pitched named AI employees — a dashboard loses to a colleague), **Salesforce acquiring Fin** (the generic front-door layer is consolidating into platforms; the defensible seat is the vertical workflow *role*).

**🛰️ Frontier scout** — *Sharpest read:* three new truths: the brain **closed the truth-maintenance loop** (Bernard maintains a current worldview, not an archive); the capture instrument **compounds and can be seeded from outside demand** (the #1857 coverage-goal mechanism accepts injected goals); and **the market repriced the output** — AI Overviews sit on ~25% of US queries and crush organic CTR 34–58%, the AI local pack often surfaces only 1–2 businesses, while Tebra's voice assistant and the Salesforce–Fin absorption **commoditize the patient phone front door Run 2 aimed at.** Meanwhile everything the brain knows exits only through social/blog/GBP renders; `search-queries.js` + `SeoOpportunities.jsx` still measure Google *clicks* — a dying KPI; nothing Bernard publishes is machine-readable to the AIs that now answer patients. **The moat is live; it just faces inward.** *Boldest move:* **compile the practice brain into the Practice Answer Graph** — a public, versioned condition×question×city answer corpus (schema.org MedicalWebPage/FAQ markup, llms.txt, a public answers API) written strictly in the clinician's captured words, with `captionFidelity.js` promoted to a **hard publish gate** on this surface; a nightly Mythos-class pass holds the ENTIRE tenant in one 1M-token window and flags contradictions; when supersession confirms a change of mind, an **outward sweep regenerates or retracts every stale published answer** — *no competitor maintains published truth; Blaze publishes and forgets.* Close the demand loop: a weekly Profound-style probe asks the frontier AIs the top condition+city questions, logs **citation share as the replacement KPI**, and every question the practice is NOT the cited answer for becomes a coverage goal injected into the interviewer — the weekly call opens with "AIs in Portland answer sciatica questions without you; give me your take." The same graph feeds an inbound cloned-voice answer line (non-diagnostic scope per the Hippocratic-Polaris template). *Outside reference:* **Profound / Semrush AI Visibility** (citation-share methodology), **Blaze.ai** (the direct leapfrog), **Tebra** (the commoditization pressure), **ElevenLabs ConvAI 2.0** (single-vendor answer line).

#### The converged verdict

**Run 1: a colleague you talk to. Run 2: a presence that's always there. Run 3: Bernard takes the job.** What last week's shipments changed is that the presence now has something to be present *with* — a live brain that maintains the practice's current worldview and a ledger that knows each clinician better every week. All seven reads land on the same diagnosis of what remains: that intelligence is **silent** (nothing discloses the ledger; the supersession moment dies in a Settings card), **stateless** (14 crons wake, act once, die — nothing reads a judge score and acts on it across ticks), and **inbound-only** (the entire capture system waits for a browser tab). The converged through-line — the rung above Run 2 — is that Bernard must stop being a presence the practice *visits* and become **a colleague the practice *employs***: a persistent, named agent holding the producer seat `requireRole` already defines, disclosing what it has learned at the moment of effort, originating the weekly call itself, answering for every promise in an auditable ledger with read-back verification, editing by instruction inside one judgment stream, and compiling the brain outward into the public answer AIs cite.

#### The frontier bet

**"The Standing Producer" — Bernard takes the seat.** One persistent, Mythos-class agent per workspace as a **named member of the practice** — "Bernard, Producer" rendered in `Members.jsx` next to the humans, holding the producer role that `requireRole` already defines and that is functionally vacant in every clinic — running the content operation end to end. Fable 5's server-side compaction + memory tool make this literal, not cron cosplay: a session keyed by `workspace_id` that never ends, holding the entire tenant corpus — interview summaries, `brand_brief`, supersession ledger, cadence history, every clinician's relationship ledger — in one window.

Five fusions, none dropped: **(1) The nervous system** [F2] — invert the cron topology: crons become sensors inserting into an `agent_inbox`; the producer wakes per event, decides with full-corpus context, acts through today's internals as tools (grounding becomes universal because the agent IS the caller), and closes the judge loop — draft → score → self-critique → regenerate until 8+ — so `voice_fidelity_score` finally *gates* instead of decorating. **(2) The initiative** [F1] — the producer originates a real SIP phone call at the cadence slot: no app, no Clerk; GPT-Realtime-2 mint with remote MCP against Bernard's own routes. **(3) The relationship** [F18] — the call opens by disclosing the earned register, resolves pending supersessions by reading the clinician their own earlier words, and speaks the strongest moment back before hangup — effort-to-reward collapses to seconds. **(4) The accountability** [F17] — every future-tense claim becomes a commitments row; read-back verification confirms the live published artifact matches the approved render; the producer's workday renders as a colleague's standup, with severity-routed escalation terminating in an outbound call. **(5) The face and the outward voice** [F4 + F19 + F16] — `/` becomes the judgment stream of typed cards carrying grade, bake state, and practice-brain citation; a per-piece edit agent executes `change_request` comments and spoken edits over all archetypes; and the brain compiles outward into the **Practice Answer Graph** — a public, supersession-maintained corpus that makes the clinician *the answer AIs cite*, with citation share seeding the producer's own interview coverage goals.

What changes what Bernard IS: **from software a practice operates to a colleague a practice employs** — one with a name, a memory it discloses, a phone it uses first, a ledger it answers to, and a public voice it maintains as truth.

*Sequence (ambition preserved, nothing dropped to be "shippable"):* (1) **F18** — carry `interview_style_memory` + pending supersessions into the realtime mint; run the style classifier on call transcripts (closes the voice-ledger gap). (2) **F1** — GPT-Realtime-2 + SIP outbound; `cadence_policy` originates the weekly call; in-call supersession resolution + spoken moment payoff. (3) **F2** — the producer seat: persistent per-workspace session, crons→`agent_inbox` sensors, judge-gated regenerate loop, `approve.js` dispatching server-side, Bernard rendered in `Members.jsx`. (4) **F4 + F19** — `/` becomes the judgment stream; the edit agent consumes `change_request` comments and spoken edits; SlideEditor/VideoEditor demote to render targets. (5) **F17** — Promise Ledger with read-back verification and severity-routed escalation. (6) **F16** — Answer Graph published under the fidelity hard gate; citation share replaces clicks in `SeoOpportunities.jsx` and seeds interview coverage goals. The bet is the whole arc: **an employee, not a tool.**

---

### ⟳ Run — 2026-07-10

> **⚠️ CORRECTION (2026-07-10, same day) — this run was grounded on a STALE worktree; its central finding is WRONG.** The grounding `Explore` swept the worktree at its branch base (`ca76dfdf` / #2068), which was **21 commits behind `origin/main`** (verified: `git rev-list --left-right --count HEAD...origin/main` → `0 21`), so it never saw today's shipments (#2069–#2089). Run 4's headline — *"Bernard built the body but forgot the mouth; F1 completely untouched, zero SIP code"* — is **false**: **F1 (the outbound phone call), F13, F18, and F20 all SHIPPED today.** Verified on `origin/main`: `api/_lib/twilioSip.js` + `outboundCall.js` + `callTranscript.js` + `api/_routes/producer/outbound-call.js` + `api/_routes/webhooks/{openai-realtime,twilio-recording,twilio-status}.js` + `src/components/home/WeeklyCallHero.jsx` (**F1**, #2069/#2076); `api/_lib/{analyzeVideoWindow,scoreMomentsVisual,visualNominate}.js` (**F13**, #2072/#2078/#2087); `src/components/home/RelationshipCard.jsx` (**F18**, #2074); `api/_routes/producer/request.js` + `api/_lib/producer/draftOnTopic.js` + `src/components/producer/AskBernardBox.jsx` (**F20**, #2073/#2077). The build-task chips this session spawned for F1/F13/F18/F20 were therefore **redundant** (the work was already merged; their sessions self-deleted). **Read the panel reads below as a record of the stale-grounding mistake, not as current state** — the corrected re-evaluation is in the register (§3) and drift log (§4) under this same date. **Lesson: ground the frontier panel against `origin/main` (fetch first, check `git rev-list --count HEAD..origin/main`), never a worktree that may predate recent merges.**

**Why now (off-cadence, on purpose — the ceiling moved again).** Eight days after Run 3, ~150 commits merged. Run 3's engine (**F2**, the Standing Producer) shipped for real — not cosmetically. **Grounding:** a very-thorough `Explore` sweep of `api/_routes/cron/agent-tick.js`, `api/realtime-session.js`, `interviewStyleClassifier.js`, `autoPublishGate.js`, `producer/regradeContentItem.js`, `content-item-comments.js`, `App.jsx` routing, `Members.jsx`, `practiceMemoryRag.js`, and the recent git log, with load-bearing claims verified by direct file read (not memory). Honest caveat (§5) stands.

**The target, from the live read — what changed.** Run 3 named five fusions to build "The Standing Producer." **Three of five are now real, verified in code, not aspiration:** (1) **The nervous system [F2] shipped** — `agent_inbox` is a real durable table with claim/sweep/dispatch, `MAX_ATTEMPTS`, stranded-job recovery, and dispatch kinds (`revise_content_item`, `regrade_content_item`, `predraftWeek`, `authorAnswersForGaps`); its own header calls it "the Standing Producer's heartbeat." (4) **The face, partially [F19]** — `change_request` comments are no longer dead-ended: `backfillChangeRequests()` scans and enqueues them automatically, dispatched by `reviseContentItem.js`. And **the judge loop closed to a real hard gate** — `autoPublishGate.js` fails closed on four signals (voice fidelity ≥7.0, similarity ≥0.65, consent, no QC flags); a new `voice_audit.gate` state machine (`held`/`passed`/`escalated`) attempts one automated repair before handing off to a human via `needs-you.js`. And **Bernard has a producer seat in `Members.jsx`** — a real card, "Bernard · always on / paused," linking to `/producer`, labeled in-code "Standing Producer Phase 0." **The other two fusions — (2) The initiative [F1 voice] and (3) The relationship [F18] — are completely untouched.** `api/realtime-session.js` still mints `gpt-realtime`; there is zero SIP/outbound-call code anywhere in the repo; `interview_style_memory` still only feeds the *next text/voice-chat interview*, never a call Bernard originates. And **the stream-as-`/` inversion Runs 1–3 all called for never happened** — `App.jsx` still routes `/` to the workbench `Home.jsx`; instead the app consolidated *satellite* routes (`/publish`, `/storyboard`, `/needs-media`, `/review-inbox`) into `/week`, leaving two front doors rather than one. **The new finding: Bernard built the producer's body and skipped its mouth.** It reasons, revises, regrades, and self-repairs — entirely through cron ticks and silent DB writes. It has never once spoken, called, or disclosed anything it knows to the person it works for.

#### Panel reads

**🎨 Principal product designer (IA)** — *Sharpest read:* the router just proved Run 1–3 right and wrong at once — legacy surfaces *did* collapse (4 routes now redirect into `/week`), but `/` stayed `Home.jsx`, so Bernard now has a producer with real inbox-driven judgment (`agent-tick.js`) and **nowhere for that judgment to surface as a single ranked view**. `/producer` exists as a link target from `Members.jsx` but is a *profile page for a colleague*, not the work itself. *Boldest move:* finish the inversion for real — `/` becomes the producer's actual outbox: every `agent_inbox` dispatch that resolved (`revise_content_item`, `regrade_content_item`, a `voice_audit.gate` flip to `held`) renders as a card the instant it lands, ranked by what needs a human vs. what's already shipped, with `/week`'s calendar demoted to a filter on the same stream. *Outside reference:* **Linear's Triage** (every object type, one ranked inbox — the exact pattern Bernard's own `agent_inbox` schema already mirrors server-side but never surfaces client-side); **GitHub Copilot Workspace's PR-as-conversation-with-an-agent** view. Bernard built the backend for this and stopped one screen short.

**🧠 Behavioral scientist** — *Sharpest read:* the ledger disclosure gap Run 3 flagged got *structurally worse*, not better — `interview_style_memory` now demonstrably changes how the NEXT interview opens (`interviewTactics.js` → `prompts.js` → `InterviewSession.jsx`), so Bernard is **provably learning and adapting week over week**, and still never says so. A clinician who's been interviewed 12 times has earned register, has confirmed supersessions, has a `voice_audit.gate` history — all real, all silent. Meanwhile `Members.jsx`'s "Bernard · always on" card is the single most personified surface in the app and it's static chrome, not a channel. *Boldest move:* even without voice (F1 still unbuilt), put a **weekly written "what I noticed about you" note** on `/producer` sourced directly from `interview_style_memory` deltas + resolved `voice_audit.gate` repairs — the cheapest possible version of F18, buildable today with zero new capture infrastructure, that converts eight months of silent state into felt reciprocity. *Outside reference:* **Wrapped-style periodic recap**, but *about the relationship*, not usage stats — closer to a **1:1 manager's weekly note** than a dashboard.

**🎙️ Conversation / voice UX designer** — *Sharpest read:* this is now the single largest gap in the whole system, confirmed by direct grep — **zero SIP/outbound code exists**, `gpt-realtime` is un-upgraded, and the style classifier that could make an outbound call *sound* earned has nothing to attach to because there's no call to originate. Every other organ (brain, judge, inbox, relationship state) matured in eight days; the mouth didn't move at all. *Boldest move:* stop treating F1's voice-outbound as one fusion among five — **make it the very next thing built**, because every week it's deferred, Bernard accumulates *more* silent relationship debt (per the behavioral scientist's read) with no channel to spend it through. Ship the narrowest possible version: `cadence_policy`'s existing trigger fires a real OpenAI Realtime SIP call (even at `gpt-realtime`, no upgrade required to start), opening with one disclosed line from `interview_style_memory`. *Outside reference:* **OpenAI Realtime SIP** (available since May, unused here for 8+ weeks now); the gap between "we have the intelligence" and "we have the channel" is now the single most quantifiable frontier debt in the register.

**🤖 AI/ML engineer** — *Sharpest read:* the architecture bet Run 3 made — invert crons into `agent_inbox` sensors — **worked exactly as designed**, and it's DB-state-driven (tick-based), not a resident Fable-5 session as originally proposed; that's a *pragmatic* substitution that already delivers 90% of the win (durable claim/sweep, retry, dedupe) without needing the 1M-context standing-session architecture at all. *Boldest move:* don't retrofit a persistent Mythos session onto a system that's already working via inbox+tick — instead spend that ambition on **closing RAG's remaining gap**: `practiceMemoryRag.js` is still opt-in per caller (`searchPracticeMemory` requires an explicit `query`), so `agent_inbox` dispatch handlers that don't happen to pass one still generate ungrounded. Make every `agent_inbox` dispatch kind mandatorily construct and pass a RAG query before it's allowed to call a generation primitive — a schema-level requirement, not a convention. *Outside reference:* the original Run 3 aspiration (Fable 5 standing session) turns out to be **solved more cheaply** by the shipped tick+inbox pattern — a genuine instance of "the frontier lab's answer wasn't needed, the boring durable-queue answer was better." Worth recording as evidence the panel should stay honest about architecture, not just chase newest-model appeal.

**🔧 Reliability engineer** — *Sharpest read:* `voice_audit.gate`'s `held`/`passed`/`escalated` machine and the one-shot auto-repair in `regradeContentItem.js` are exactly the "self-heals, then hands off what it can't" pattern Run 3 asked for — but it's the *second* narrow reliability primitive built this way (after `autoPublishRetry.js`), and there's still no general `commitments`/promise-ledger table unifying them. Two independent state machines (`auto_publish_state.published_channels[...]` and `voice_audit.gate`) now separately track "did the thing I promised actually happen," with separate escalation paths (`notifyPublishFailure.js` email vs. `needs-you.js`). *Boldest move:* before adding a third bespoke guard, extract the shared shape both already share (attempt-count, terminal state, escalation-once) into the general ledger Run 3 named — it's now overdue, not aspirational, since there are two real implementations to generalize FROM rather than design from scratch. *Outside reference:* still **Stripe Events**; the difference from Run 3 is Bernard now has enough live examples to reverse-engineer the right schema instead of guessing one.

**🔁 Workflow/ops designer** — *Sharpest read:* `Members.jsx`'s Bernard card is a genuine milestone — a named agent literally sits in the roster next to Whitney, AJ, Alli — but `/producer` is a read-only status page, and the `change_request`→`agent_inbox`→`reviseContentItem.js` loop, while automated, still only *executes* instructions a human already wrote in a comment thread. Nothing yet lets a human hand Bernard an open-ended ask ("cover the disc-herniation angle better this week") the way you'd delegate to a real coordinator. *Boldest move:* extend `agent_inbox` with a dispatch kind sourced from a **free-text delegation box on `/producer`** — "ask Bernard to..." — routed through the same claim/sweep/retry machinery already proven reliable for `change_request`. This is the cheapest possible next step given what's already built: no new infrastructure, just a new inbox producer. *Outside reference:* **Linear for Agents**' assignable-agent pattern, now literally analogous to what `Members.jsx` + `agent_inbox` already look like — just missing the input box.

**🛰️ Frontier scout** — *Sharpest read:* the market kept moving in the same direction Run 3 named (AI-employee positioning, citation-share repricing organic search), and Bernard's `agent_inbox` + `Members.jsx` combination is now — verified in code — a *more concrete* instance of "AI employee with a seat and a workday" than most of the vendors selling that story with a marketing page and a chatbot behind it. The gap is that Bernard's employee currently **cannot pick up a phone or update its own public voice** — the two moves (F1 outbound, F16 Answer Graph) that would make the "employee" claim externally legible are both still `Open`, unchanged since Run 3. *Boldest move:* ship F1's narrowest outbound-call slice specifically as a **demonstration of the employee framing** — even a single weekly disclosure call is a categorically different sales claim than "software with an inbox," and Bernard now has all the internal state (relationship ledger, resolved gates, regrades) to make that first call substantive rather than a gimmick. *Outside reference:* Tebra/Podium's "AI employee" pricing keeps normalizing this category for the exact SMB buyer Bernard serves — the risk of standing still on F1 is a competitor phones a clinic before Bernard does, despite having a thinner brain behind the call.

#### The converged verdict

**Run 3 named five fusions to build the Standing Producer. In eight days, Bernard built the body and forgot to build the mouth.** All seven reads converge on the same asymmetry: the nervous system (`agent_inbox`), the hard judge gate, the self-repair loop, and the literal producer seat in `Members.jsx` are real, verified, working code — Bernard now reasons, revises, and regrades autonomously, exactly as bet. But the two fusions that make any of that *legible and felt* to the human it serves — originating a call (F1) and disclosing what it has learned (F18) — are untouched, and the stream-as-home-screen inversion that would surface its work never happened either. The producer has a job, a memory, and now a body of finished work each week. It has never once told anyone what it did.

#### The frontier bet

**"Bernard picks up the phone."** The single highest-leverage move available right now is not a new capability — it's *connecting two systems that already both exist*. `interview_style_memory` (adaptive, earned, currently silent) and `agent_inbox`'s resolved work (regrades, repairs, revisions — currently invisible) both already contain everything a disclosure call needs to say. The realtime infrastructure (`api/realtime-session.js`, `cadence_policy`) already exists and only needs an outbound leg. Nothing else in the register is this close to done while remaining this unbuilt. Ship the narrowest possible version — `cadence_policy` triggers a real SIP-outbound call at the existing `gpt-realtime` mint, opening with one disclosed line ("since we last talked, I noticed X, and here's what I shipped this week") — and the Standing Producer stops being a colleague that works silently in the next room and becomes one that calls to tell you what it did. Every other open item (Answer Graph, promise-ledger generalization, `/` inversion, free-text delegation) compounds on top of this, but none of them require Bernard to *exist socially* to the person it works for. This one does, and it's the cheapest bet on the board given what's already shipped.

*A sketchable sequence (ambition preserved, nothing dropped to be "shippable"):* (1) wire SIP outbound on the existing `gpt-realtime` mint, triggered by `cadence_policy` — no model upgrade required to start [**F1**, narrowed]; (2) open every call with a disclosed line sourced from `interview_style_memory` + the week's resolved `agent_inbox` dispatches [**F18**, now buildable from real data instead of aspirational]; (3) surface the same resolved-dispatch stream visually at `/` so the inversion Runs 1–3 asked for finally lands, informed by what `agent_inbox` already proves is worth showing [**F4/F19**, unblocked]; (4) generalize `voice_audit.gate` + `auto_publish_state`'s shared shape into the promise ledger, now reverse-engineerable from two live implementations instead of designed cold [**F17**]; (5) extend `agent_inbox` with a free-text "ask Bernard to..." dispatch kind on `/producer`, the cheapest way to make delegation two-way [new, folds toward **F8**]. The bet is still the whole arc — but for the first time, every step is a wire between two things that already exist, not a new capability to invent.

---

### ⟳ Run — 2026-07-16

**Why now (six days after Run 4 — all three re-run triggers fired at once).** (a) *The ceiling moved:* ~70 commits since Run 4's correction, and the register's top open items finished their arcs — **F16 completed within a day of Run 4 naming its gaps** (Phase 3 supersession-maintained public corpus + RETRACT path, #2094 + Movebetterco #108, E2E prod-verified; Phase 4 Google AI Overviews as the third citation engine, #2095, live-API validated), the **first genuinely patient-facing surface went live** (the movebetter.co homepage concierge — generate-grounded on the blog+answer corpus, gap questions flowing back into Bernard's `topic_backlog` through the HMAC `topic-signal` webhook, #2097), and the human-judgment layer got its keystone (words-approval hard gate at every dispatch primitive + the per-story monitor, #2107–#2112). (b) *Frontier releases:* GPT-5.6 (Jul 9), ChatGPT Work (Jul 9), GPT-Live full-duplex voice (Jul 8, API waitlist), gpt-realtime-2.1 (Jul 6), Fable 5 redeployed (Jul 1), ElevenLabs Speech Engine (June), OpenAI Dreaming V3 (Jun 4). (c) *The market repriced the ACTION layer:* Google I/O — Search's agent will **call and book local businesses**, rolling out summer 2026 (i.e., now); UCP extended toward services; OpenAI's Instant-Checkout retreat settled the pattern ("discover in AI, transact with the merchant"); Jane — Move Better's own scheduler — opened a gated partner API. **Grounding:** very-thorough `Explore` sweep of live `src/` + `api/` on a worktree verified at parity with `origin/main` (`git rev-list --left-right --count HEAD...origin/main` → `0 0` — the Run 4 lesson applied), a web-verified outward scan (every date checked), the Vigil-tracked SaaS-productization spec read, and load-bearing claims hand-verified (`gpt-realtime` in both mints; `/` → `Home.jsx`; `syntheticBroll.js` provenance). Honest caveat (§5) stands.

**The target, from the live read — what changed.** For the first time, the three-run arc is **structurally complete on two of its three faces**: the inward colleague is real (F1 call live-verified 7-10 + F2 producer + F18 card + F20 delegation), and the outward truth layer is real (F16's full loop: gate → go-live → supersession sweep → retract → 3-engine citation scoreboard — "self-healing published truth," the thing Run 4 called the differentiator left to build, is built). Verified new since Run 4: **universal RAG grounding** — every producer lane now constructs a retrieval query before generating (`draftAtom.js`, `reviseContentItem.js`, `regradeContentItem.js`, `draftAnswer.js`; grounded-only escalation in `draftOnTopic.js:112-125`) — Run 4's last F2 gap closed quietly. What did NOT change: `/` still renders the workbench (`App.jsx:599`, fifth consecutive run); judgment still splits across six surfaces; the call still opens with a *receipt* (published titles via `readShippedThisWeek`, `outboundCall.js:47-99`) rather than a disclosed relationship; both mints still say `gpt-realtime` (`twilioSip.js:28`, `realtime-session.js:42`); the outbound trigger is still manual-pilot-only ("Coming soon" on `WeeklyCallHero.jsx:79-81`); F17 is now SEVEN bespoke promise machines with email-only notifications; and the fresh post-#2082 live-call re-test hasn't happened. **The new finding: the patient side of Bernard exists but cannot transact.** The concierge answers; the answer pages get cited; `/link` renders the booking URL; GA4 counts Book-Now clicks — and then the funnel exits Bernard entirely at a bare Jane deep link. Meanwhile the world's agents are about to start calling and booking clinics directly. **One constraint-hygiene flag for Q:** `api/_lib/syntheticBroll.js` (Runway text-to-video b-roll, merged 5-28 in #953) predates the 6-21 ⛔ generative-video ruling and is still wired into `generate-package.js` when no real clips exist — dormant only while `RUNWAY_API_KEY` stays unset; delete it or confirm-dark it.

#### Panel reads

**🎨 Principal product designer (IA)** — *Sharpest read:* the router held at `/` = workbench for a fifth run, but the app just built the inversion's proof pieces without noticing: `WordsApproval.jsx` (#2110) is a perfect judgment card — ONE screen, ONE question ("does this sound like you?"), evidence face-up, consequences server-enforced — and the rebuilt `AssetsPane` monitor (#2107) is "review and watch, not compose and publish" as a design stance. The stream doesn't need inventing; its card types are shipping one at a time (words keystone, held-gate repair, superseded answer, needs-you escalation, failed publish, winner declaration). *Boldest move:* assemble them — `/` becomes **the desk**: one ranked queue of typed judgment cards over everything (`agent_inbox` resolutions, gates, supersessions, gap questions from the concierge, booking-attribution beats), each answerable in one gesture, `/week` demoted to a calendar lens, the whole queue walkable by voice on the weekly call. *Outside reference:* **Linear Triage**; **ChatGPT Work** (Jul 9) — the consumer default is now "review an agent's finished artifacts," so a workbench home reads a generation behind.

**🧠 Behavioral scientist** — *Sharpest read:* disclosure finally shipped — but as a **card** (`RelationshipCard.jsx` speaks register, angles, session count) while the *moment of effort* stays undisclosed: the call's opener is a shipped-titles receipt (`outboundCall.js:94-99`) and the register directive is internal stage-direction (`interviewTactics.js:61`), never said aloud. And a brand-new reward sits unclaimed: the citation scoreboard is live with `cited=false` everywhere — meaning the FIRST flip to `cited=true` is coming, and nothing will celebrate it. *Boldest move:* an **identity engine on real consequences** — when a tracked question flips cited, Bernard tells the clinician *in the call*: "you are now the answer AIs give for sciatica in Portland — because of what you said on June 14," attributing the win to the exact interview sentence; plus steal OpenAI Dreaming V3's pattern (Jun 4): a readable **"what Bernard believes about your practice"** page with correction affordances — the brain made legible is reciprocity AND a better supersession funnel. *Outside reference:* **Dreaming V3's memory-summary page**; Strava/Wrapped identity mechanics.

**🎙️ Conversation / voice UX designer** — *Sharpest read:* the mouth exists, spoke once (7-10, live-verified), and then froze one generation behind: both mints still `gpt-realtime` while **gpt-realtime-2.1** (Jul 6) ships configurable reasoning + better mid-call tool-calling at −25% p95 — a drop-in upgrade — and **GPT-Live** (Jul 8) resets the bar to full-duplex (backchannels, listens-while-speaking); the trigger is still a manual admin POST ("manual trigger only," `outbound-call.js:8-10`) with the hero promising "Coming soon"; and the post-fix fresh-call re-test is still owed. *Boldest move:* **one brain, three mouths** — (1) clinician mouth: cadence-originated calls on gpt-realtime-2.1 that open with the spoken relationship and resolve supersessions by voice; (2) patient mouth #1: the concierge gains a voice via **ElevenLabs Speech Engine** (June — chat agent → voice agent in a prompt); (3) patient mouth #2: an inbound answer line — **Hippocratic×UNC** (June) proved patients will book with a clinic-side AI voice. Join the GPT-Live API waitlist this week. *Outside reference:* **gpt-realtime-2.1**, **GPT-Live**, **ElevenLabs Speech Engine**, **Hippocratic × UNC**.

**🤖 AI / ML engineer** — *Sharpest read:* the architecture arguments are over — grounding is universal (verified across every lane), the judge gates for real, supersession sweeps published artifacts, and the industry just ratified the whole design: **OpenAI wound down self-serve fine-tuning** (May), settling that per-tenant voice lives in **context + evals + memory, not weights** — Bernard's practice-brain + fidelity-judge IS the winning pattern; don't build distillation. The remaining gaps are perceptual and evaluative: F13 still *samples stills* over ffmpeg range-seeks while **Gemini 3.5 Flash reads 1–2 hours of video natively for cents** (batch −50%, cache −90%), and no dispatch is ever graded against outcomes. *Boldest move:* **give the brain a body of evidence** — watch the ENTIRE media back-catalog with native full-video understanding (moments, claims, b-roll, on-camera energy indexed once, pennies per asset), run a nightly **dream pass** (Managed Agents' dreaming/outcomes pattern, May 6) that re-synthesizes the belief graph and writes the readable belief page, and **outcome-grade every producer dispatch** (published? cited? booked?) so the Standing Producer learns its own taste. Keep a model-fallback seam — Fable 5's 19-day suspension (Jun 12–Jul 1) is the concentration-risk lesson. *Outside reference:* **OpenAI FT wind-down**, **Claude Managed Agents dreaming/outcomes**, **Gemini 3.5 Flash video economics**, **GPT-5.6 Sol** token efficiency for inner loops.

**🔧 Reliability engineer** — *Sharpest read:* the bespoke-guard census went UP again — **seven** independent promised-vs-actual machines now (`auto_publish_state`, `voice_audit.gate`, `words_approved_at`, `dispatch_state`, `broll_status`, three stuck-sweeps, `agent_inbox.status`) with no unifying ledger; notifications are email-only (`twilio-status.js` explicitly rules out SMS; no push anywhere); and the producer's `escalation_email` lane defaults ON with **no sender in existence** (`producer/config.js`) — a promise with no mouth, the exact class this seat exists to catch. Plus the constraint-hygiene find (pre-ruling Runway path still wired). *Boldest move:* the **Promise Ledger** (third ask, now reverse-engineerable from seven live implementations — overdue by any standard) — and productize it: publish the gates as a **per-practice public trust scorecard** ("every word owner-approved · voice fidelity ≥ threshold · every claim traceable to your own interview · every publish read-back-verified against the live platform") the way **Hippocratic** publishes 99.9% safety over 10M calls (June) — in healthcare AI, audited trust is not plumbing, it's the sales asset. *Outside reference:* **Stripe Events**; **Hippocratic's published safety methodology**; Gemini-video read-back verification at cents.

**🔁 Workflow / ops designer** — *Sharpest read:* the SaaS axis got locked while nobody was looking at this panel — Vigil's spec (7-11): jobs-based tiers, managed analytics, beachhead = **no-marketer clinics from Q's warm network**, "automation over depth" — and the code seams already exist (`plan`/trial/Stripe migrations 035/036, `planGate.js`, billing routes) but `planGate` knows three features and the onboarding wizard is still a **form** (scan-website → synthesize → claim) in a product whose entire thesis is the interview. *Boldest move:* **Bernard onboards you by phone** — the product demos itself: a prospect leaves a number; Bernard calls, runs the shipped brand-discovery interview (#1827) as a conversation, and by hangup the workspace exists — brain seeded, first week pre-drafted, trial started, zero forms. The beachhead clinic that will never wire GA4 will absolutely take a phone call. Second head: generalize movebetter.co/answers into the **Bernard-hosted per-tenant answer hub** (Vigil's own "sleeper" option) — it solves the tenant publish path, gives every tenant the answer graph, and quietly builds F14's cross-tenant corpus. Price it like an employee, not software — outcome-based agent pricing is emerging (HubSpot, mid-2026). *Outside reference:* **Blaze's $899/mo managed tier** (the market pays for done-for-you), **HubSpot outcome-based pricing**, **Jane Developer Platform**.

**🛰️ Frontier scout** — *Sharpest read:* the ground shifted from *who do AIs cite* to **who can AIs act on** — and the window is measurably open: Google's agent will **phone and book local businesses** starting this summer (I/O, May 19); UCP is extending agent transactions into services (GML, May 20–21); OpenAI's Instant-Checkout retreat (Mar 4) settled the architecture — *discover in AI, transact with the merchant*; MCP endpoints became default business storefronts in commerce (Shopify Q1, Dynamics Jun 29); **Zocdoc×Yelp** (Apr 21) is aggregating healthcare booking into every AI surface at $2/booking; and **Jane — Move Better's own scheduler — opened a gated partner API** (JDP, Apr 1). Demand exploded (**BrightLocal: 45% of consumers now ask AI for local services, 7× in a year**) while supply is absent (**SOCi: ChatGPT recommends 1.2% of local businesses**). The competitor split is clean: ops-voice AI (Tebra/Assort/Weave) has no content/AEO story; content AI (Blaze) has no provenance and no hands. **Nobody makes a clinic agent-bookable on the strength of its own captured expertise.** *Boldest move:* claim the whole seam — the per-clinic **Agent Gateway**: one public, Bernard-hosted endpoint per tenant (MCP + UCP-shaped actions + schema.org) exposing verified practice facts, the supersession-maintained answer corpus, and REAL booking actions via JDP — so whether the patient asks ChatGPT, Google's calling agent, Perplexity Comet, or the clinic's own site, the thing that answers *and books* is the clinic's own brain, never a marketplace. Apply for JDP partner status **now**; gated APIs are moats for whoever's inside. *Outside reference:* **Google agentic calling/booking**, **UCP services expansion**, **JDP**, **SOCi 1.2% / BrightLocal 45%**, **Zocdoc×Yelp**.

#### The converged verdict

**Run 3 hired the employee. Run 4 gave it a phone. This run found the practice needs a front desk.** All seven reads converge on the same asymmetry: Bernard's two built faces — the inward colleague and the outward truth layer — both stop exactly where money changes hands. The concierge answers but can't book; the answer pages get cited but the funnel exits at a bare Jane deep link; the citation scoreboard measures visibility while the world's agents (Google's calling agent this summer, Comet, ChatGPT Health journeys) start acting on local businesses directly; and the trust machinery that would make autonomous patient-side action sellable (seven scattered promise machines) still has no ledger. The through-line the panel agrees on: **teach the presence to transact** — give the brain hands on the patient side (answer → converse → book, human-facing and machine-facing), speak the relationship it already has on the clinician side, prove every link in one ledger, and let the SaaS axis multiply it by N clinics.

#### The frontier bet

**"From cited to booked — the agent-bookable practice"** [new **F21**]. The single move that most changes what Bernard *is*: extend the practice's AI presence past the answer and through the appointment. Concretely — the live answer corpus + verified practice facts + **real booking actions** (via Jane's partner API) exposed through every door at once: the website concierge (live today) gains hands; an inbound answer line gains a voice (Speech Engine / the pre-visit clone infrastructure, non-diagnostic scope per the standing rulings); `/link` and the hosted answer hub gain one-tap booking; and a per-clinic **Agent Gateway** (MCP endpoint + UCP-shaped actions + schema.org) makes the same brain legible and actionable to Google's calling agent, ChatGPT, and Comet — so the clinic's own endpoint, not a $2/booking marketplace, is what every AI resolves to. The KPI ladder finishes: citation share → AI-referred conversations → **booked appointments attributed to captured voice** (GA4 Book-Now clicks + JDP webhooks close the join), and the attribution feeds the Standing Producer's outcome grading, so the employee learns which of its work fills the calendar.

What changes what Bernard IS: **from the practice's marketing employee to the practice's growth engine** — the thing that makes the phone ring, answers it, books it, and proves it did.

*Sequence (ambition preserved, nothing dropped to be "shippable"):* (1) apply for **JDP partner status** immediately (gated = moat) and wire the concierge's booking hand [**F21**]; (2) voice the patient side — Speech Engine on the concierge + the inbound answer line [**F12**, absorbing **F15**'s live-agent pattern]; (3) stand up the per-clinic Agent Gateway + the per-tenant hosted answer hub (Vigil's "sleeper," which also seeds **F14**'s cross-tenant corpus) [**F21** + **F16** generalized]; (4) the attribution ledger + outcome-graded producer + the public trust scorecard [**F17**, from seven live implementations]; (5) in parallel, the clinician side finishes its mouth — gpt-realtime-2.1 + cadence-originated calls that speak the relationship and celebrate the first citation flip [**F1** depth + **F18**]; (6) the SaaS multiplier — Bernard onboards new clinics by phone [**F22**]. The bet is the whole arc: **an employee whose work you can hear ringing the front desk.**

---

## 3. The ambition register

*(Carried forward and re-ranked each run. Boldest / highest-upside first. Columns:)*

*Re-ranked 2026-06-27 (Run 2): boldest **remaining** ambition first. Shipped items sink — their open upside is what ranks, not their original score. ⬆️ NEW Run 2: **F11–F15**.*

*Re-ranked 2026-07-02 (Run 3): **F6 shipped days after Run 2** (the brain is live); the judge now soft-gates the draft path; the register's F7 self-heal line went stale (guards shipped). ⭐NEW this run: **F16–F19**. F8/F10/F15 fold into the Standing-Producer arc (F2/F1/F18) rather than standing alone.*

*Re-ranked 2026-07-10 (Run 4): **F2 shipped** — `agent_inbox` durable claim/sweep/dispatch loop, `voice_audit.gate` hard-gate + one-shot self-repair, `change_request` auto-consumption, and a real Bernard seat in `Members.jsx`. ~~F1 and F18 are the board's clear #1.~~*

*⚠️ **CORRECTED 2026-07-10 (same day)** — the Run 4 re-rank above was written on a **stale worktree (21 commits behind `origin/main`)** and is WRONG. **F1, F13, F18, and F20 all SHIPPED today** (see the ⚠️ correction banner in §2 for the file list). Corrected statuses below. The genuinely-open frontier is now entirely **OUTWARD-facing** — **F16 (Answer Graph)** and **F12 (patient-facing presence)** — plus DEPTH/PROOF on what shipped (F1 spoken-relationship + GPT-Realtime-2; F13 real-footage verification). Everything shipped today faces inward at the clinician; nothing yet faces patients or the answer-engines.*

*Re-ranked 2026-07-16 (Run 5): the genuinely-open frontier is now the **transaction layer**. **F16 → Shipped** (full arc: gate · go-live · supersession sweep + retract E2E · Google AIO third engine). **F12 → Building** (movebetter.co concierge live as the first patient-facing seed). F2's last remainder (universal RAG grounding) verified CLOSED. ⭐ NEW: **F21** (the agent-bookable practice — the Run 5 frontier bet) and **F22** (voice-first onboarding). Editorial consolidation: the register's two F1 rows merged into one; F9/F10/F15 collapsed into a single absorbed-items row. Constraint-hygiene flag on F3 (pre-ruling Runway b-roll path).*

| ID | Bold move | Lens | Upside | Status | Outside reference / note |
|---|---|---|---|---|---|
| **F21** | **From cited to booked — the agent-bookable practice**: a per-clinic public **Agent Gateway** (MCP endpoint + UCP-shaped actions + schema.org) exposing verified practice facts, the supersession-maintained answer corpus, and REAL booking actions via Jane's partner API; the concierge / answer line / `/link` gain one-tap booking; an attribution ledger closes content → citation → conversation → **booked appointment** and feeds producer outcome-grading | 🛰️ / 🔁 / 🤖 | 🚀 | ⭐ NEW — **Open, the Run 5 frontier bet.** Seeds live: concierge (gap flywheel into `topic_backlog` verified — `webhooks/topic-signal.js`), `/link` page, GA4 Book-Now click tracking, 3-engine citation scoreboard. Missing: the booking seam (**apply for Jane Developer Platform partner status NOW** — gated = moat), the gateway itself, the attribution join | **Google I/O agentic calling+booking of local services** (5-19, rolling out summer 2026); UCP services expansion (GML 5-20); Instant-Checkout retreat → "discover in AI, transact with the merchant" (3-04); MCP storefronts default in commerce (Shopify Q1, Dynamics 6-29); **Zocdoc×Yelp** $2/booking aggregation (4-21); **SOCi 1.2% recommended vs BrightLocal 45% asking** |
| **F12** | **Patient-facing AI presence** — the practice brain answers, books, and triages patients in the practice's voice; flags content gaps | 🤖 / 🛰️ | 🚀 | **Building — first seed LIVE**: the movebetter.co homepage concierge (generate-grounded on 46 blogs + 14 answers, fixed non-diagnostic rails, gap questions → `topic_backlog` via HMAC `topic-signal`, #2097). Remaining: give it a voice (ElevenLabs **Speech Engine**, June — chat agent → voice agent in a prompt), booking hands (→F21), per-tenant generalization. Grounded-only; never invents patient stories | **Hippocratic×UNC** inbound scheduling (June — patients book with clinic-side AI voice); Assort Health $120M@$1.2B (6-24); ChatGPT Health enterprise workspace (wk of 7-07) — care journeys now start inside assistants |
| **F18** | **The Spoken Relationship** — the call opens with the earned register, resolves supersessions by voice, speaks the strongest moment back; celebrate the **first citation flip** with sentence-level attribution | 🧠 / 🎙️ | ⬆️ | Open (written card shipped #2074) — verified this run: the call opener is still a shipped-titles **receipt** (`outboundCall.js:47-99`) plus an internal register stage-direction (`interviewTactics.js:61`), not disclosure; no in-call supersession or moment payoff. All source data is live — this is purely prompt-assembly on a working call | Aron "fast friends"; **Dreaming V3's readable memory page** (6-04) — the visual sibling: a "what Bernard believes about your practice" page with corrections (→F6) |
| **F17** | **Promise Ledger + Reliability Steward** — every future-tense claim a `commitments` row; read-back verification; severity-routed notifications; **productized as a public per-practice trust scorecard** | 🔧 | ⬆️ | Open — census UP: **seven** bespoke promised-vs-actual machines (`auto_publish_state`, `voice_audit.gate`, `words_approved_at`, `dispatch_state`, `broll_status`, three stuck-sweeps, `agent_inbox.status`); notifications email-only (no push/SMS); the producer's `escalation_email` lane defaults ON with **no sender existing** (`producer/config.js`) — a promise with no mouth | **Stripe Events**; **Hippocratic publishes 99.9% safety over 10M calls** (June) — the healthcare-AI trust playbook: the gate becomes the sales asset; Gemini-video read-back verification at cents |
| **F14** | **Cross-practice network intelligence** — privacy-preserving aggregate of what drives citations/bookings across similar practices; seeds each tenant with proven winners in its own captured voice | 🔁 / 🛰️ | ⬆️ | Open — repriced UP: the SaaS axis is locked strategy (Vigil 7-11; Stripe + `planGate` seams already wired, migrations 035/036) and the per-tenant hosted answer hub (→F16) builds its shared condition×question corpus | Aggregate-anonymized ONLY — never crosses tenant isolation |
| **F22** | **Voice-first onboarding — the product demos itself**: a prospect leaves a number; Bernard calls, runs the brand-discovery interview as a conversation, and by hangup the workspace exists — brain seeded, first week pre-drafted, trial started, zero forms | 🔁 / 🎙️ | ⬆️ | ⭐ NEW — Open. Every seed is shipped: brand-discovery interview (#1827), the outbound call stack (F1), onboarding API routes (`scan-website` / `synthesize` / `claim`); Vigil's onboarding-path spec is queued. The no-marketer beachhead won't wire a console but will take a phone call | **Blaze $899/mo managed tier** (the market pays for done-for-you); **ChatGPT Work** (7-09) normalizes the agent-does-it-all default; **HubSpot outcome-based agent pricing** as the commercial frame |
| **F4+F19** | **The desk** — `/` becomes one ranked queue of typed judgment cards (gates, supersessions, escalations, concierge gap questions, booking beats), each one-gesture; the per-piece edit agent inline and by voice; `/week` demotes to a calendar lens | 🎨 / 🤖 | ⬆️ | Building — `/` STILL `Home.jsx` (`App.jsx:599`, fifth consecutive run); judgment split across six surfaces. But the card types are shipping piecemeal: **WordsApproval keystone** (#2110 — one screen, one question) and the story **monitor** (#2107 — review-not-compose) prove the pattern; F20 free-text delegation shipped | **Linear Triage**; **ChatGPT Work** artifact-review UX — reviewing an employee's finished work is the consumer default now; Descript Underlord (the edit-agent half) |
| **F1** | **The Weekly Call** — outbound + inbound realtime voice as the practice's only required input | 🎙️ / 🧠 | 🚀 | **Shipped + live-verified** (7-10 pilot call → approved blog; v1.1/v1.2 + #2082/#2132/#2137 hardening). Remaining depth, verified this run: both mints still `gpt-realtime` (`twilioSip.js:28`, `realtime-session.js:42`) — **gpt-realtime-2.1 (7-06) is a named drop-in upgrade**; trigger still manual-pilot-only ("Coming soon" hero); fresh post-fix live re-test owed; **join the GPT-Live API waitlist** (7-08 — full-duplex resets the bar). Hosts F18-spoken next | **gpt-realtime-2.1** (reasoning-configurable speech-to-speech, −25% p95); **GPT-Live** full-duplex; Tebra/Podium AI-employee calling norms |
| **F16** | **Practice Answer Graph** — public, supersession-maintained answer corpus; citation share as the KPI | 🛰️ / 🤖 | 🚀 | **Shipped — the full arc is live** (fidelity hard gate #2091 · per-workspace go-live #2092 · supersession sweep + RETRACT #2094/mbco#108, E2E prod-verified · Google AIO third engine #2095, live-API validated). Remaining: a per-version audit trail (rows mutate in place — verified), auto coverage-goal injection (still operator-clicked `queue_goal` — verified `citation-question.js`), the **per-tenant hosted answer hub** (Vigil's "sleeper" — generalize movebetter.co/answers; seeds F14), prompt-corpus probing (track real user prompts à la Brand Radar vs hand-picked questions) | **Seer**: uncited brands −67% CTR, cited +35% organic / +91% paid; **Google's llms.txt ruling** (6-15): zero ranking effect — schema + content is the lever; Ahrefs Brand Radar ($699+/mo) / Semrush AI Toolkit ($99/mo) — Bernard bundles per-clinic what these sell standalone |
| **F2** | **The Standing Producer** — persistent named agent holding the producer seat | 🤖 / 🔁 | 🚀 | **Shipped** — and Run 4's last remainder CLOSED, verified: **universal RAG grounding** (every lane builds a retrieval query; grounded-only escalation `draftOnTopic.js:112-125`). Remaining: the escalation-email sender (lane ON, sender absent — see F17), and **outcome-grading on dispatches** (grade each by published / cited / booked — the learning loop F21's attribution enables) | **Claude Managed Agents** dreaming / outcomes / orchestration (5-06) — the platform now sells the primitives Bernard hand-built; **GPT-5.6 Sol** economics for the inner loops |
| **F6** | **The practice brain** — retrieval + supersession truth-maintenance grounding everything | 🤖 | ⬆️ | **Shipped.** New depth this run: a nightly **dream pass** re-synthesizing the belief graph + a readable **"what Bernard believes about your practice"** page with correction affordances — disclosure AND a better supersession funnel. The industry ratified the architecture: **OpenAI wound down self-serve fine-tuning** (May) — per-tenant voice lives in context + evals + memory, not weights; don't build distillation | **Dreaming V3** (6-04); **Managed Agents dreaming** (5-06); the FT wind-down |
| **F13** | **Video-native moment understanding** — score what the camera sees | 🤖 / 🛰️ | ⬆️ | **Shipped** (blend #2072 + badge #2078 + visual-nominate #2087). CORRECTED this run: the visual re-rank runs **ungated** inside every segment detection (`segmentDetect.js:389-395`); only the nominator is flag-gated (`F13_VISUAL_NOMINATE_WORKSPACES`). Proof on real uploads still unobserved. Leapfrog open: **Gemini 3.5 Flash native full-video** (~cents, 1–2h in one call, batch −50%) replaces ffmpeg still-sampling → watch the entire back-catalog once; doubles as F17's read-back verifier | Gemini video economics (6-2026); OpusClip ClipAnything. Subsumes F9 |
| **F5** | **Prompt-native on-brand graphics** — brand-locked image gen replaces hand-built compositing | 🎨 / 🛰️ | ⬆️ | Open — record CORRECTED this run: **zero image-gen code in-repo** (the 6-26 spike was scratch-only; `brandRender` / `compose-photo` are sharp/satori compositing of existing photos). Becomes the producer's image tool when built | **Nano Banana 2 Lite** (June); gpt-image-2 (on the account); Recraft V4 editable-vector output |
| **F7** | **Compounding payoff + proactive reliability** | 🧠 / 🔧 | ⬆️ | Building — the reliability half escalates into **F17**; the payoff half into **F18** plus the **first-citation champagne moment** (the scoreboard is live with cited=false everywhere — the first flip is coming; nothing will celebrate it unless built) | Wrapped / Strava recaps; Knock / Resend push |
| **F8** | **Content OS for the practice** — assignment, ownership, ops agent | 🔁 | ⬆️ | Open — still folds into F2: Bernard owns every item; humans are delegated *to* (the outbound call is the handoff channel) | Linear for Agents; Figma multiplayer |
| **F20** | **Free-text delegation** — "ask Bernard to..." | 🔁 | ✨ | **Shipped** (draft-on-topic scope, #2073/#2077). Remaining: general intent-routing beyond draft-on-topic | Linear for Agents' assignable-agent pattern |
| **F3** | **The Moment-Miner** (generative video **rejected**) | 🛰️ | 🚀 | **Shipped**; generative video stays **Closed** (authenticity ruling, holds). ⚠️ **Constraint-hygiene flag (this run):** `api/_lib/syntheticBroll.js` — Runway text-to-video b-roll, merged **5-28** (#953), three weeks *pre-ruling* — is still wired into `generate-package.js` when no real clips exist; dormant only while `RUNWAY_API_KEY` stays unset. **Q: delete the path or confirm-dark it** | `memory/feedback-no-ai-generated-video.md` |
| **F11** | ~~**Ambient "Bernard in the room"**~~ | 🎙️ / 🛰️ | 🚀 | **🛑 SHELVED (Q, 2026-06-27)** — permanently parked; **do not re-propose** until Q manually re-allows | See **⛔ Standing constraints** + `memory/feedback-no-treatment-audio-capture.md` |
| **F9 · F10 · F15** | *(absorbed)* quality scoring · best-time scheduling + seeds · live in-call co-pilot | — | ✨ | F9 → folded into **F13** · F10 → absorbed into **F1/F2** (the initiative clock) · F15 → folds into **F1/F18** on the clinician side and **F12**'s live answer line on the patient side | — |

---

## 4. Drift log

*Append each run. The question that matters: is the ceiling rising — is Bernard becoming a more ambitious product, and did the last frontier release open a new move?*

| Date | What changed since last pass |
|---|---|
| 2026-06-20 | Panel created. Unbounded sibling of the pragmatic product panel: time/cost are non-constraints, ranked by ambition, 7th Frontier-scout seat + outward mandate on all experts, grounds-in-live-app-then-goes-unbounded. No runs yet. |
| 2026-06-20 | **Run 1 executed.** First panel convened against the live app. Converged verdict: shift Bernard from *tool you operate* → *colleague you talk to*. Frontier bet named: **"The Weekly Call → a published quarter."** Key live-read finding — the three most ambitious moves (realtime voice `PhoneCall.jsx`, voice-judge `captionFidelityRubric.js`, RAG `corpus/*`) are **already scaffolded but gated/stubbed**; the ceiling is switched *off*, not absent. Register seeded F1–F10 (3 × 🚀 transformative). Outward scan named **Blaze.ai** as the direct competitor; Bernard's voice-native intake + practice-brain are the wedge. Baseline for "is the ceiling rising?" — next run diffs against this. |
| 2026-06-27 | **Run 2 executed — one week after Run 1, off-cadence by design** (triggered by an unusually fast ceiling rise, not the 6–8wk clock). **The ceiling is rising fast: 327 commits merged, and 3 of Run 1's "switched-off scaffolds" got switched ON** — F1 realtime voice → *voice-for-all + voice-first default* (#1823/#1826), F4 unified editor *live* (#1682/#1804/#1805), F5 brand discovery *shipped* (#1827) + image-gen spike (6-26); also `/usage`+`/admin` (#1831), publish-failure surfacing/retry (#1813/#1840), voice-clone opt-out+authz (#1801/#1806/#1811), AI-SDK v7 (#1803). **New finding: the frontier moved up a level** — from "turn on the scaffolds" (Run 1) to "build the 3 layers that don't exist yet": ambient capture, autonomous-by-default, two-way patient presence. **New frontier bet: "The Practice's AI Identity Layer"** (apex = F12 patient presence; input = F11 ambient; engine = F2 autonomy). Register grew **F11–F15** (2 new 🚀, 2 new ⬆️, 1 ✨); shipped items (F1/F3/F4) re-ranked down to remaining-upside, F5/F7 → Building. **Generative-video frontier remains closed** — Q's authenticity ruling holds regardless of Sora 2 / Veo 3.5 gains. Outward scan added two new reference classes: **healthcare ambient AI** (Abridge/Nabla/Suki/Dragon Copilot) and **patient-facing agents** (Decagon/Sierra/Fin). **⛔ Post-run, same day:** F11 (treatment-audio capture) **permanently SHELVED by Q** after investigating the Jane AI Scribe import — consent too muddy (documentation-scoped + "never shared"); recorded in ⛔ Standing constraints + `memory/feedback-no-treatment-audio-capture.md`. |
| 2026-07-10 | **⚠️ Run 4 CORRECTION (same day).** Run 4's grounding was done on a **stale worktree 21 commits behind `origin/main`**, so its headline finding — *"Bernard built the body but forgot the mouth; F1 completely untouched"* — is **false**. Verified against `origin/main`: **F1 (the outbound phone call — real Twilio SIP + OpenAI Realtime bridge + 3 webhooks + `WeeklyCallHero`), F13 (3-part video-native scoring), F18 (RelationshipCard on Home), and F20 (AskBernardBox → draftOnTopic) ALL shipped today** (#2069–#2087). The build-task chips spawned for these were redundant. **Corrected frontier read:** Run 3's entire "Standing Producer + Weekly Call" arc is now BUILT and connected — the inward-facing colleague is real. The genuinely-open frontier is now entirely **OUTWARD**: **F16 (Practice Answer Graph — be the answer AIs cite)** and **F12 (patient-facing presence)**, plus DEPTH/PROOF on today's shipments — the shipped call is shallow (still `gpt-realtime`, F18-disclosure is a written card not spoken in-call, no in-call supersession/moment payoff) and F13 is flag-gated + unverified on real footage. **Is the ceiling rising? Yes, faster than the panel can track — the register's #1 shipped the same day it was named.** **Process lesson recorded: ground the panel against `origin/main` (fetch + `git rev-list --count HEAD..origin/main`), never a possibly-stale worktree.** |
| 2026-07-10 | **F16 Phase 1+2 shipped mid-session — the 4th same-day redundant-spawn catch.** After F16 was planned this session (gate→go-live first), **#2091 (voice-fidelity hard gate on answers, mig 169) + #2092 (per-workspace `answer_publish_enabled`, mig 170) merged to `origin/main` while the session was still open** — caught at wrap by re-checking `origin/main` (branch had drifted 21→24 behind). The spawned Phase 1–2 build task was therefore redundant, exactly like the F1/F13/F18/F20 spawns earlier. **F16 remaining after today: Phase 3 (self-healing — versioning + supersession-triggered retraction) + Phase 4 (AI Overviews via SerpApi — key staged in Vercel — + auto coverage-goals).** The recurring meta-lesson of this whole run: in a repo this fast, re-verify `origin/main` immediately before EVERY spawn/plan/ship step, not just at grounding. |
| 2026-07-10 | **Run 4 executed — eight days after Run 3, ~150 commits merged.** Verified via direct code read (not memory): **F2 (Standing Producer) shipped for real** — `agent_inbox` durable claim/sweep/dispatch table confirmed in `cron/agent-tick.js` ("the Standing Producer's heartbeat" per its own header), `autoPublishGate.js` hard-gates on 4 signals, new `voice_audit.gate` state machine self-repairs once then escalates via `needs-you.js`, `change_request` comments auto-consumed (no longer dead-ended), and `Members.jsx` renders a real Bernard card ("Standing Producer Phase 0" in-code) alongside human staff. **Architecture note:** it shipped as a DB-state tick-driven pattern, not the resident Fable-5 session Run 3 proposed — the cheaper pattern won and the panel recorded this as a case where the frontier-model answer wasn't needed. **The gap that widened: F1 (voice-outbound) and F18 (spoken disclosure) are completely untouched** — zero SIP/outbound code found anywhere on direct grep, `api/realtime-session.js` still mints `gpt-realtime`, and `interview_style_memory`'s read-path (confirmed live, feeds the next interview's prompt) has no channel to disclose through. The stream-as-`/` inversion Runs 1–3 all called for **still didn't happen** — `App.jsx` still routes `/` to `Home.jsx`; instead the app consolidated 4 satellite routes into `/week`. **New frontier bet: "Bernard picks up the phone"** — connect two already-shipped systems (relationship ledger + resolved producer work) via the one missing wire (SIP outbound on the existing realtime mint), rather than build a new capability. Register: **F1 promoted to #1** (was folded into Run 3's F2 sequence, now stands alone as the clear top-upside open item); F2 → Shipped; F19 → Building; F17 downgraded from "design cold" to "generalize from two live implementations"; new **F20** (free-text delegation) added as the cheapest next step. |
| 2026-07-02 | **Run 3 executed — five days after Run 2; both re-run triggers fired at once.** (a) *The ceiling moved again:* **F6 went Open → LIVE end-to-end within days** (#1843–#1846 — pgvector RAG + recency half-life + weekly supersession cron + judge + confirm queue), the interviewer became adaptive with a per-clinician **relationship ledger** (#1857/#1859), and — unrecorded by any prior run — **the voice judge now soft-gates the draft path** (`draft.js` scores at generation time, regenerates once on a below-GATE score; Runs 1–2's "the judge gates nothing" is half-false) and **real self-heal guards shipped** (`autoPublishRetry`, two limbo sweepers, once-per-transition failure email). (b) *Frontier releases:* the **Claude 5 family** (Fable 5 Mythos-class: 1M ctx, server-side compaction, memory tool — a whole tenant in one never-ending session; Sonnet 5 agentic autonomy at commodity price; first-party multi-agent orchestration), **OpenAI Realtime SIP calling + remote MCP**, **ElevenLabs ConvAI 2.0** (RAG-in-voice-loop + outbound, same vendor as the clinician clones), and **AI Overviews repricing local discovery** (~25% of queries; clicks → citation share as the KPI). **Converged diagnosis:** the intelligence is now real but *silent, stateless, and inbound-only*. **New frontier bet: "The Standing Producer" — Bernard takes the job**: a persistent named agent in the (vacant) producer seat — crons → `agent_inbox` sensors, judge loop closed to a hard gate, SIP-outbound weekly call, ledger disclosed in-call, promise ledger with read-back verification, `/` as the judgment stream, and the brain compiled outward into the **Practice Answer Graph**. Register grew **F16–F19** (1 new 🚀 F16, 3 new ⬆️); F6 → Shipped; F8/F10/F15 folded into the producer arc; F12 repriced (front door commoditizing — Salesforce–Fin, Tebra — the moat shifts to the answer layer). Panel mechanics upgrade: seats ran as **independent parallel agents** + synthesis, load-bearing code claims hand-verified. |
| 2026-07-16 | **Run 5 executed — six days after Run 4; all three re-run triggers fired at once.** ~70 commits. Verified: **F16 completed its full arc within a day of Run 4 naming the gaps** (P3 supersession sweep + RETRACT, E2E prod-verified #2094/mbco#108; P4 Google AIO third engine #2095, live-API validated) — "self-healing published truth" is BUILT; **first patient-facing surface LIVE** (movebetter.co concierge, gap flywheel → `topic_backlog` #2097); words-approval hard gate at every dispatch primitive + per-story monitor (#2107–#2112); **universal RAG grounding confirmed** (Run 4's last F2 remainder closed quietly); SaaS direction locked in Vigil (7-11) with Stripe/`planGate` seams already in code. Outward scan repriced the ACTION layer: **Google's agent will call and book local businesses starting this summer** (I/O 5-19); UCP → services; Instant-Checkout retreat settled "discover in AI, transact with the merchant"; **Jane opened a gated partner API** (JDP); SOCi 1.2% of local businesses recommended vs BrightLocal 45% of consumers asking (7×/yr); gpt-realtime-2.1 + GPT-Live reset the voice bar. **New frontier bet: "From cited to booked — the agent-bookable practice" (⭐F21)** — the presence learns to transact: concierge gains booking hands (JDP), per-clinic Agent Gateway (MCP/UCP/schema.org), attribution ledger citation→conversation→booking feeding producer outcome-grading. ⭐F22 (voice-first onboarding) added; F12 → Building; F16 → Shipped; two F1 rows merged. **Constraint hygiene:** pre-ruling Runway b-roll path (`syntheticBroll.js`, merged 5-28, ⛔ ruling 6-21) flagged for delete-or-confirm-dark. **Is the ceiling rising? Yes — and for the first time the open frontier sits OFF Bernard's own surfaces: the transaction layer between the practice and the world's agents, with a window (empty AI-rec shelf, gated JDP, Google's summer rollout) that is time-sensitive in a way no prior bet was.** |

---

## 5. The honest caveat (always)

This panel is **expert *simulation*, not user *observation*** — sharp at "what would the best people and best tech obviously do," blind to "what real clinicians actually trip on." And ambition without grounding is just noise: the live-app read is what keeps the boldness specific. Weight findings as "very likely the right direction," not "proven." The only ground truth remains a real staffer using the app.

---

*Keep file (human-authored instrument — treat like source, per the `.claude/` scratch-vs-keep convention). Sibling: `product-panel-audit.md` (pragmatic). Run via `/frontier-panel`.*
